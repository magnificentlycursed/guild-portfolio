# VSDD Suite Compatibility Policy

The suite's compatibility commitment to projects that have adopted it.

**Reason this file exists:** registered as G-120 in [`suite-development/GAP-ANALYSIS-LOG.md`](suite-development/FINDINGS-INDEX.md) by Review 41 (Solution Architect lens). The forward-only constraint applied across G-88, G-89, G-91, G-92, G-93, G-95 IS the suite's compatibility policy — but it had been distributed across individual gap rows rather than documented as a coherent commitment. This file makes the policy explicit so a new user can verify the suite's backwards-compatibility posture without reading the gap registry.

---

## The policy in one sentence

**Forward-only compatibility:** suite changes apply to projects starting after the change date; projects whose first IAR run predates the change retain the pre-change shape and are not retroactively migrated.

## What this means in practice

- **Projects that adopted the suite before a given change** keep using the pre-change conventions. Their `iterative-adversarial-refinement/` subdirectories (legacy naming), single-file-per-domain review logs (legacy structure), or prior primer file names continue to be valid records. The suite does not require those projects to migrate.
- **Projects that adopt the suite after a given change** use the post-change conventions. New `vsdd-suite/` subdirectories, per-domain index + per-session-file review logs, and the current primer file names.
- **Both shapes coexist in the same portfolio.** The portfolio's pre-commit hook regex covers both shapes; primer/domain prompts are version-independent in their content (the conventions they reference adapt to the project's adopted-version state).

## Version anchors

| Version | Date | What changed |
|---|---|---|
| v0.7.2 | 2026-05-20 | Review 66 (Phase 5 primer refinements from bookmark-cli execution). 4 findings Addressed: **G-173** (Surface A.0 audits module/package docs alongside DESIGN.md for purity claims — bookmark-cli's `src/lib.rs:1-7` was the canonical case), **G-174** (Surface B gains 5th "unviable" disposition class for type-system-rejected mutations), **G-175** (Tool-install upfront cost paragraph + first-session log preamble convention), **G-176** (Surface A explicitly names the dev-dep add prerequisite per language; dep-add lands as a separate commit before property-test commits). **2 bookmark-cli-side findings (B1, B2)** surfaced and held pending operator authorization (forward-only G-89 carve-out: bookmark-cli's first-layer-gate-close predates 2026-05-20). No breaking changes against the v0.7.1 contract — refinements within the existing Phase 5 primer surfaces. v0.7.1-authored projects do NOT need to migrate; prior Phase 5 audits (none exist in any project yet — no project has run Phase 5 under v0.7.0+ except the bookmark-cli validation sandbox) remain valid under the prior conventions. |
| v0.7.1 | 2026-05-20 | Review 65 (cold-context primer validation of Review 64 work via subagent). 9 findings Resolved + 3 Deferred. **Resolved (refinements to Phase 5/6 primers):** Surface B "evaluation scope" replaces "threshold"; per-surface sycophancy checks for Surfaces A/C/D in addition to B; `fast-check` Surface A vs C distinction explicit; forward-only anchored to date `2026-05-20` instead of v0.7.0; new Surface A.0 (purity-boundary verification preamble); Phase 6 `milestone close` ID-capture pattern documented; Phase 6 Dimension 2 explicit verification step that disposition tables exist in cited PHASE-5-LOG entries; anonymization-aware attestation via git-commit-hash signature; 2b retroactive-Red-Gate label extended to cover post-MVR Phase 5 discovery via `(Phase 5 source)` qualifier. **Deferred:** G-170 (untestable-by-design disposition class for consistency check), G-171 (Phase 6 strategy grammar inline + portfolio-intent verification path), G-172 (project-level Implementation MVR rollup for multi-layer projects). No breaking changes against the v0.7.0 contract — all changes are refinements within the existing primer surfaces. v0.7.0-authored projects do NOT need to migrate; Phase 5/6 work under v0.7.0 conventions remains valid. |
| v0.7.0 | 2026-05-20 | Review 64 (Phase 5 + Phase 6 implementation): **G-55 Addressed** via new `primers/5-formal-hardening.md` (4 surfaces — A property-based, B mutation testing, C fuzzing, D formal proof — strictly optional). **G-54 Addressed** via new `primers/6-convergence.md` (per-dimension MVR + cross-dimension consistency check). Every VSDD phase the methodology defines is now owned. New VDD-IAR Alignment dims 13 (Phase 5 discipline) + 14 (Phase 6 four-dimensional convergence). New routing rows + labels `route:phase-5` / `route:phase-6` in `primers/4-feedback-integration.md`. New project artifacts: `vsdd-suite/PHASE-5-LOG.md` (per-project cumulative) + `vsdd-suite/PHASE-6-CONVERGENCE.md` (per-project terminal). Forward-only: projects whose first layer-gate close predates v0.7.0 retain prior "Phase 5/6 skip unless safety-critical" framing — pre-v0.7.0 projects do NOT need to retro-fit Phase 5/6 work. Capstone + production intents starting after v0.7.0 must declare Phase 5/6 strategies in `DESIGN.md` § Project intent (G-162 enforcement; not new — promoted from v0.6.0; targets now point at real primers). The strategy declaration's `planned` form maps to specific Surfaces (5) and dimensions (6) named in the new primers. **Migration:** none required for pre-v0.7.0 projects. Capstone-intent projects authored under v0.6.0 with `**Phase 5 strategy:** planned — <scope>` declarations that referenced abstract scope ("formal verification of pure functions") can update the scope to reference Surface labels ("Surface A + Surface D"); no breaking change either way. G-162 declarations under v0.6.0 (without the now-existing primers as targets) remain valid records. |
| v0.6.0 | 2026-05-19 | Review 63 (multi-lens SO/SA/TW suite review): 8 F-findings Addressed + 2 Deferred. **G-160:** Phase label `Phase 1a` → `Phase 1a+1b` (cross-cutting Python sweep; `git mv primers/1a-spec-crystallization.md → primers/1ab-spec-crystallization.md`). **G-161:** new VDD-IAR Alignment dim 12 enforces Phase 2c refactor-scope-creep discipline. **G-162:** new § Phase 5 / Phase 6 strategy declaration at capstone+ intents (explicit-skip pattern generalized from G-96's Phase 2c annotation). **G-163:** scaffold-project.sh registers only activated domain prompts. **G-164:** scaffold-project.sh registers `supplements/` tagged `vsdd-suite-supplement`. **G-165:** HTML comment markers on 6 G-106 sample-output blocks. **G-166:** POSIX-compatible shell prerequisite + Windows workaround. **G-167:** known-limitations section in crosslink-contract.md (4 v0.8.0 surface limitations enumerated). Forward-only: project review logs that reference "Phase 1a" alone under prior suite conventions remain valid (G-89). Projects scaffolded under v0.5.0 affected at the artifact level — primer file rename, so any project script citing `primers/1a-spec-crystallization.md` by path must update to `primers/1ab-spec-crystallization.md`; primer-loading-by-paste is unaffected (the primer's content is the operative surface; the filename is the dispatch path). DESIGN.md under v0.5.0 at capstone+ intent now require `**Phase 5 strategy:**` + `**Phase 6 strategy:**` declarations to close the gate; pre-v0.6.0 capstone-intent projects are not retroactively failed (forward-only on first-layer-gate-close-date). **Migration:** if a project's automation cites `primers/1a-spec-crystallization.md` by path, update to `primers/1ab-spec-crystallization.md`; if a capstone-intent project's DESIGN.md was authored under v0.5.0 without Phase 5/6 declarations, add the two lines before the next layer-gate close. G-54 + G-55 remain Open — Phase 5/6 ownership unchanged. |
| v0.5.0 | 2026-05-19 | Review 62 (G-96 + G-106 + G-146 closures): documentation-and-tooling cluster. **G-96** harmonized the suite's sub-phase taxonomy with the VSDD whitepaper — `vsdd-suite/primers/1b-decomposition.md` → `vsdd-suite/primers/1c-decomposition.md` (tracks whitepaper Step 1c Spec Review Gate); new `vsdd-suite/primers/2c-refactor.md` (whitepaper Step 2c Refactor). Phase 4 routing labels updated: `route:phase-1b` → `route:phase-1c`; new `route:phase-2c`. **G-146** wired crosslink knowledge auto-injection into `templates/scaffold-project.sh` — primers registered with tag `vsdd-suite-primer`, domain prompts with tag `vsdd-suite-domain`. **G-106** embedded verbatim sample outputs for the worked-example crosslink commands; fixed three CLI-surface defects in the prior README (`milestone add/show/close` take numeric IDs; `swarm gate` requires `swarm init --doc` first; `swarm review --doc` is OUTPUT path). Forward-only: project review logs that reference "Phase 1b" decomposition under the prior suite convention remain valid records (G-89 narrative-preservation policy). Projects scaffolded under v0.4.0 are unaffected at the artifact level — the rename is internal to suite primer filenames; project-side `vsdd-suite/` trees that don't reference the primer files don't need migration. **Migration for projects with explicit primer-path references:** if a project's docs or scripts cite `primers/1b-decomposition.md` by path, update to `primers/1c-decomposition.md`; if a project's automation uses `route:phase-1b` crosslink labels, new findings should use `route:phase-1c`. Existing `route:phase-1b`-labelled findings remain valid records. **Knowledge-page versioning** (G-159, Deferred): projects that registered primers under v0.4.0 (none yet exist — G-146 wiring is new in v0.5.0) would need `crosslink knowledge import --overwrite` to refresh — but this concerns is forward-looking; no v0.4.0-scaffolded crosslink-knowledge state exists to migrate. |
| v0.4.0 | 2026-05-19 | Review 61 (G-149 closure): suite-development artifact rename to align with project-template naming. `SUITE-REVIEW-INDEX.md` → `SUITE-DEVELOPMENT-REVIEW.md`; `GAP-ANALYSIS-LOG.md` → `FINDINGS-INDEX.md`. Mechanical sed sweep updated all forward-facing internal cross-references; historical narrative files (review-log entries, CHANGELOG entries, prior version anchors here) preserve the old names in prose with markdown link targets pointing to new file paths. Suite can now serve as a worked example of its own template-defined naming (dogfood-correct). No breaking changes against this policy's project-facing surface — projects scaffolded from `templates/` already used the new names; the rename is internal to suite-development/. **Migration for projects with their own suite-side cross-references:** if a project's vsdd-suite/ tree links to `suite-development/SUITE-REVIEW-INDEX.md` or `suite-development/GAP-ANALYSIS-LOG.md` (rare — these are contributor-facing artifacts), update those links to the new names. |
| v0.3.0 | 2026-05-17 | Review 40 + 41 onboarding overhaul: README Prerequisites / Quickstart / "Bringing the suite into your project" sections; `templates/` directory; crosslink-contract.md; this COMPATIBILITY.md; AI-tool data-flow posture in Prerequisites. Forward-only — completed projects retain prior README and prior absence of templates. |
| v0.2.0 | 2026-05-17 | Review 38 + 39 post-ITC restructure: `iterative-adversarial-refinement/` → `vsdd-suite/` directory rename; `prompts/` → `primers/` with phase-prefixed filenames; suite-meta into `suite-development/`; README user/contributor split; `prompts/implementation.md` → `2a-red-gate.md` + `2b-implementation.md`; per-domain index + per-session file review-log structure (G-89). Forward-only — `bookmark-manager/` and `issue-tracker-cli/` retain their inner `iterative-adversarial-refinement/` paths and single-file-per-domain logs. |
| v0.1.0 | 2026-05-06 | Baseline at the close of Review 36 — IAR suite with full domain set, contributor governance (gap log, suite-review index, review-log/, suite-development.md primer), language and interface supplements, crosslink integration discussed but not yet first-class. Treated as the earliest tagged version because Review 36's anonymization-hook landing made the suite stable for portfolio use. |

Version tags v0.1.0, v0.2.0, v0.3.0, v0.4.0, v0.5.0, v0.6.0, v0.7.0, v0.7.1, v0.7.2 are stub tags applied retroactively to anchor the forward-only policy to specific suite states. Future versions follow semantic versioning: PATCH for fix-only changes; MINOR for additive non-breaking changes; MAJOR for breaking changes against this compatibility policy.

## Breaking change definition for the suite

A change is **breaking against this compatibility policy** when:

- Existing project review logs become syntactically invalid against the suite's governing standard (e.g., a required field renamed or removed from the per-review entry preamble).
- Existing project artifact paths break — the renames in v0.2.0 are an example: `iterative-adversarial-refinement/` projects could not be expected to migrate; the suite kept the legacy path supported via the forward-only carve-out, so the change was NOT breaking against this policy.
- A primer's `## Prompt` text changes in a way that would invalidate review logs filed under the prior version (e.g., a finding classification removed from a domain's allowed schema while existing logs contain that classification).
- The contract file [`crosslink-contract.md`](crosslink-contract.md) experiences a documented crosslink breaking change that the suite cannot accommodate without breaking the worked example.

A change is **non-breaking** when:

- New domains, dimensions, supplements, or primers are added (additive).
- An existing prompt is reworded for clarity without changing the schema or classification universe.
- A documentation gap is closed (e.g., the Review 40 onboarding overhaul — adding sections to README without changing what projects under prior versions had to do).
- The renames in v0.2.0 — paired with a forward-only carve-out that preserves the prior shape.

The forward-only convention is the suite's primary mechanism for shipping structural changes without breaking compatibility: changes apply to future adopters; existing adopters keep working in the prior shape; the suite documentation acknowledges both shapes.

## Deprecation process

When a suite artifact must be deprecated (renamed, removed, restructured):

1. **Announce in CHANGELOG.md** under the relevant Unreleased section, naming the deprecated artifact and the replacement (or "no replacement; remove from new projects").
2. **Document the forward-only carve-out** in the relevant gap registry row, naming what existing projects retain.
3. **Update the suite's own artifacts** for the new shape; pre-existing project artifacts are not modified.
4. **Run a registry-walk suite review** when the deprecation completes, confirming the policy was applied consistently across all suite-side updates.
5. The deprecated artifact remains conceptually valid for completed projects indefinitely — there is no "remove old artifact" step. Forward-only compatibility means old conventions accumulate but never break.

## Cross-references

- [`suite-development/GAP-ANALYSIS-LOG.md`](suite-development/FINDINGS-INDEX.md) § Reactivation triggers — the bundled "after `issue-tracker-cli` completes" trigger was a coordinated deprecation event; G-88/G-89/G-91/G-92/G-93/G-95 closures all applied this policy.
- [`crosslink-contract.md`](crosslink-contract.md) — separate contract for the suite's dependency on crosslink.
- [`CHANGELOG.md`](CHANGELOG.md) — the per-release record that anchors version tags to specific change sets.
- [`domains/role/SOLUTION-ARCHITECT-REVIEW.md`](domains/role/SOLUTION-ARCHITECT-REVIEW.md) — SA Extended dims 14, 15, 16, 20 (breaking-change definition, versioning strategy, backward compatibility, deprecation process) are the dimensions this file applies to the suite.
