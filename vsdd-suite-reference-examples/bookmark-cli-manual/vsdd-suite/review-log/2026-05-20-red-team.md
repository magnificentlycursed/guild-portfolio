# Red Team Review — 2026-05-20

[Index](../RED-TEAM-REVIEW.md)

---

## Review 1 — 2026-05-20 19:30Z

**Scope:** Phase 3 IAR Round 1 — first Red Team cold-context adversarial pass against bookmark-cli Layer 1 (capstone-intent activation per the extended-pool criteria). Reviewed [`Cargo.toml`](../../Cargo.toml), [`src/main.rs`](../../src/main.rs), [`src/lib.rs`](../../src/lib.rs), [`tests/bookmarks.rs`](../../tests/bookmarks.rs), [`manual-tests/layer-1.md`](../../manual-tests/layer-1.md), [`README.md`](../../README.md), [`DESIGN.md`](../../DESIGN.md), and [`Cargo.lock`](../../Cargo.lock). Sibling [Security](../../../../vsdd-suite/domains/role/SECURITY-REVIEW.md) round for the same date had not yet been committed when this Red Team session ran — this entry is the independent adversarial pass; sequencing flip (Red Team without prior Security log) is noted in Session note below.

**Session note:** Cold-context session — this reviewer did not participate in authoring DESIGN.md, the implementation, or the QE / SA rounds. Sycophancy-compensation: the Red Team supplement entries in [`../../../../vsdd-suite/supplements/rust.md`](../../../../vsdd-suite/supplements/rust.md) § Red Team (path traversal, panic-as-DoS, supply chain, `unsafe`, integer overflow) and [`../../../../vsdd-suite/supplements/json.md`](../../../../vsdd-suite/supplements/json.md) § Security (depth-limit, string-length-limit, document-size-limit) were enumerated as a checklist BEFORE re-reading the source — every dimension was exercised against the code rather than reasoned-around. Sequencing note: the Security round for 2026-05-20 had not landed at session-start (no `2026-05-20-security.md` in [`./`](.)); per the primer 3 § Sequencing guidance "Run Security before Red Team — Security ensures controls exist; Red Team verifies they hold" this would normally be a flip, but the parallel-dispatch carve-out applies (the suite supports concurrent cold sessions, and the absence of a Security log makes this round a fully-independent adversarial pass rather than a verification overlay). Any Security findings landing later that overlap with this entry will be reconciled via Coordination in the next round.

**Source:** `domain-raised` — every Finding below was elicited by applying the [Red Team domain](../../../../vsdd-suite/domains/role/RED-TEAM-REVIEW.md) dimensions and the [Rust](../../../../vsdd-suite/supplements/rust.md) + [JSON](../../../../vsdd-suite/supplements/json.md) supplement § Red Team / § Security entries against the source files; no director-raised observations interrupted the cold pass.

**Posture:** Adversarial. Threat actors considered: (a) another local user on a shared workstation with FS-read access to the bookmark store's parent directory; (b) a hostile script the user pastes or runs that calls `bm add <attacker-URL>` or that pre-stages files at the storage path; (c) a polluted shell environment (compromised `.bashrc` / `.envrc` / direnv / inherited container env) that sets `$BOOKMARK_CLI_DB`; (d) a co-resident process racing the user's `bm add` between load and save. Crown jewel: integrity of the user's bookmark file + the user's terminal session (which is hijackable via output rendering of attacker-influenced stored data). Networked attackers are out of scope — `bm` makes no network calls.

**Threat model summary (attack-surface enumeration per Dim 2):**

| Surface | Trust boundary | Adversary-controlled? |
|---|---|---|
| `bm add <url>` positional arg | CLI shell → process | Yes — operator may paste or pipe URL from untrusted source |
| `bm list` (no args) | CLI shell → process | No direct input, but reads stored URLs that may be adversary-influenced |
| `$BOOKMARK_CLI_DB` env var | Shell env → process | Yes if shell init scripts are compromised, or if `bm` runs in a polluted env |
| Storage file at resolved path | FS → process (load) and process → FS (save) | Yes if attacker has write access to the path's parent dir, or if a co-resident process races between load and save |
| Stdout / stderr written to terminal | Process → terminal emulator | Adversary content is the URL field of stored bookmarks; rendered via `println!("{} {}", ts, url)` at `src/main.rs:75` |
| Cargo dependencies | crates.io registry | All sources `registry+https://github.com/rust-lang/crates.io-index` per `Cargo.lock`; no git deps, no path overrides |

---

### Resolved

*(none — this round filed five new findings; none had fixes applied in-session.)*

---

### Accepted risk

**Finding 1 — `$BOOKMARK_CLI_DB` ambient authority over write target (Dim 5 — business-logic abuse; Rust supplement § Red Team — path traversal)**

**Owner:** solution-owner
**Status:** raised
**Blocked by:** *(none)*

[`src/main.rs:32-36`](../../src/main.rs) (`store_path`) takes `$BOOKMARK_CLI_DB` verbatim into a `PathBuf` with no canonicalization, no allowlist, and no warning. [`src/lib.rs:67-69`](../../src/lib.rs) (`BookmarkStore::save`) then calls `std::fs::write(path, json + "\n")`, which truncates the target file if it exists and creates it (with default mode) if not. An adversary who can set the env var in the user's shell (compromised `.bashrc`, malicious `.envrc` picked up by direnv, inherited env from a container or a wrapped `sudo -E`) can therefore have `bm add <innocuous-url>` truncate any file the user has write permission to. The destructive payload is the serialized JSON document, not the URL string — but the destruction itself is the attack: a user-run `bm add` overwrites `~/.ssh/known_hosts`, a project's `.env`, a working-tree file, or any other operator-writable path.

**Reproducer (paths abstracted per primer 3 § Confidentiality-aware citation):**
```sh
echo "important content" > <target>
BOOKMARK_CLI_DB=<target> bm add https://innocuous.example
# <target> now contains the serialized BookmarkStore JSON; original content lost.
```
The "URL" the attacker captures is irrelevant; the file overwrite is the primitive.

**Why this is real, not theatrical:** the env var is documented in DESIGN.md § Scope and non-goals as the project's entire config surface ("Configuration file — environment variable + sensible default is the entire config surface"). DESIGN.md does not name the trust boundary on that env var. The Rust supplement § Red Team — path traversal entry explicitly calls out that `Path::join` and bare-path I/O do not sanitize; the same concern applies one level up — accepting an env var directly into a destructive I/O sink without canonicalize-and-verify is the same defect class.

**Why Accepted risk rather than Raised to SO:** the trust-delegation IS the design choice for a single-user local CLI — refusing to write outside an allowlist would break the documented "use any path you want" affordance. The accepted boundary is "the env var is the operator's trust surface; if it's compromised, the attacker already has worse primitives (arbitrary command execution via the shell that set the var)." The Red Team's job is to make that boundary explicit, not to dispute it. Risk owner: solution-owner. The finding is recorded so the trust delegation is auditable; future layers (Layer 2 + 3, or any multi-user repositioning) should re-evaluate.

**Classification:** Accepted risk. Risk owner: solution-owner. The env var is the operator's declared trust surface; the attack assumes a prior compromise (env-var injection) that already grants worse primitives. Cross-reference: this finding overlaps with the symlink-follow primitive in Finding 6 — combining a polluted env var with a pre-staged symlink at the resolved path compounds the attack (the env var picks the symlink; the symlink redirects the write). The combination does not change the trust model but does illustrate Dim 9 (chained vulnerabilities) — recorded for future Security/Red Team rounds to address jointly.

---

**Finding 2 — Unbounded URL length is a memory + disk DoS primitive (Dim 11 — automated attack resilience; JSON supplement § Security — string length limit + document size limit)**

**Owner:** solution-owner
**Status:** raised
**Blocked by:** *(none)*

DESIGN.md § Edge case catalog states: "Very long URL (10K+ chars): accepted. No length cap." The implementation matches — [`src/main.rs:43-47`](../../src/main.rs) checks only `url.is_empty()`, then [`src/lib.rs:76-81`](../../src/lib.rs) (`BookmarkStore::add`) pushes the `String` unconditionally, [`src/lib.rs:67-70`](../../src/lib.rs) (`save`) calls `serde_json::to_string_pretty` (which allocates a copy) then `std::fs::write` (which writes the full payload to disk).

An adversary who can run `bm add "$(yes A | tr -d '\n' | head -c $((10*1024*1024*1024)))"` (10 GiB string) drives the process to (a) `clap` accepts the arg (no length cap on clap's String parsing); (b) `BookmarkStore::add` clones into the Vec; (c) `save` allocates the serialized JSON (~10 GiB + escape overhead); (d) `fs::write` writes that to the storage path, filling whatever filesystem holds the path. End states: OOM-kill mid-allocate, or successful 10+ GiB write filling the user's disk. Re-running `bm list` on the resulting store then re-allocates 10+ GiB on read. The 10 GiB figure is illustrative; the attack scales to whatever the user's shell will pass through ARG_MAX (typically ~128 KiB to a few MiB on Linux/macOS, but `xargs -0` or stdin-pipe shaped wrappers easily defeat that). Even a 100 MiB URL is a meaningful DoS for a tool advertising "captures URLs at the terminal."

The JSON supplement § Security calls out "Untrusted JSON parsing — string length limit" + "overall document size limit" as the standard defenses. None is applied here — no cap pre-write, no cap pre-read, no per-load streaming bound.

**Why Accepted risk:** the explicit non-goal in DESIGN.md ("No length cap") is the trust delegation — the user-is-responsible doctrine extends to "don't paste a 10 GiB URL." The Red Team's job is to surface that the spec-acknowledged accepted edge case IS adversarially weaponizable, not to dispute the design choice. A multi-user or untrusted-input repositioning would warrant a length cap; at single-user Layer 1, the operator accepts the risk.

**Classification:** Accepted risk. Risk owner: solution-owner. Cross-reference: DESIGN.md § Edge case catalog "Very long URL" + § Non-goals "URL validation beyond non-empty — accept any string."

---

**Finding 3 — Concurrent-write race adversarially weaponizable for write loss (Dim 5 — business logic abuse — race conditions)**

**Owner:** solution-owner
**Status:** raised
**Blocked by:** *(none)*

DESIGN.md § Edge case catalog: "Concurrent writes: out of scope; not a multi-process tool. Single user, single shell session." The implementation has no file locking, no atomic-rename pattern, no `flock(2)` — [`src/lib.rs:60-72`](../../src/lib.rs) (`save`) does a plain read-modify-write: `BookmarkStore::load` (read), `add` (mutate in memory), `BookmarkStore::save` (write-truncate). Two concurrent `bm add` invocations race: A reads state S, B reads state S, A writes (S + bookmark_A), B writes (S + bookmark_B). Result: A's bookmark is silently lost; the user has no signal that anything went wrong.

Adversarial weaponization: an attacker with the ability to run a shell command (e.g., via a wrapped `git pre-commit` hook the user installed, a malicious script the user pastes, a co-resident process) can deliberately race the user's `bm add` invocations to wipe legitimate bookmarks selectively. The attacker doesn't need to write malicious content — silent loss is the primitive. For a tool the user trusts to remember URLs, silent loss is a confidentiality-of-recall failure (the URL the user added is gone; they may believe it persisted).

**Why Accepted risk:** DESIGN.md explicitly excludes the multi-process scenario; the trust delegation is "single user, single shell session." Surfacing the adversarial framing makes the boundary explicit. Atomic-rename (`tempfile` + `rename`) is the conventional Rust pattern but would extend implementation surface; the spec's acceptance is the operator's call.

**Classification:** Accepted risk. Risk owner: solution-owner. Cross-reference: DESIGN.md § Edge case catalog "Concurrent writes: out of scope." The Red Team raises this as Accepted risk (not Dismissed) so the trust-delegation is auditable — a future Layer where multi-process semantics enter scope (e.g., Layer 3 `bm import` reading concurrently with `bm add` writing) must revisit this without inheriting the silent-loss acceptance.

---

### Raised to SO

**Finding 4 — Terminal-escape injection via stored URL rendered by `bm list` (Dim 7 — client-side attack chains — stored output injection)**

**Owner:** solution-owner
**Status:** raised
**Blocked by:** *(none)*

[`src/main.rs:74-76`](../../src/main.rs) renders each stored bookmark with `println!("{} {}", bm.timestamp.to_rfc3339(), bm.url)` — the URL field is written verbatim to stdout with no escape-sequence stripping, no quoting, no filtering. The URL field is a JSON-deserialized `String` ([`src/lib.rs:28-31`](../../src/lib.rs)); JSON natively supports `` escapes that deserialize to the raw byte `0x1b` (ESC). A bookmark stored with URL `"]0;PWNEDhttps://example.com"` will, on `bm list`, emit `ESC ] 0 ; P W N E D BEL` to the terminal — an OSC 0 sequence that sets the terminal's window title. The same channel admits OSC 8 (hyperlink masquerade: render a clickable link whose visible text is one URL but whose target is another), `\x1b[2J\x1b[H` (clear screen + cursor home), iTerm2 OSC 1337 (file/clipboard manipulation), OSC 133 (shell-integration prompt confusion), and DCS sequences depending on terminal emulator.

**Attack chains this enables (Dim 9):**
1. **Stored output spoofing.** Adversary adds a URL `"https://evil.example\x1b[F\x1b[Khttps://bank.example"` — on `bm list`, the cursor-up + erase-line sequence rewrites the immediately-prior line to show `https://bank.example`. The user copies what they see; the actual stored URL is the attacker's.
2. **Title-bar reconnaissance / phishing setup.** Adversary sets the terminal title to mimic a legitimate process (e.g., `sudo password prompt`), increasing the chance the next keystroke goes to attacker-controlled buffer in a multiplexed terminal session.
3. **OSC 8 hyperlink phishing.** OSC 8 lets the visible link text differ from the URI; rendered by modern terminals (iTerm2, kitty, WezTerm, gnome-terminal recent). A `bm list` line can display `https://benign.example` but launch `https://evil.example` on click.

**How a URL containing escape bytes lands in the store:** three plausible paths — (a) the operator pastes from a phishing source (`bm add "$(curl evil.example/url)"`); (b) another process writes the JSON file directly (the file is world-readable per Finding 5 and at least owner-writable; a co-resident attacker process or a malicious shell wrapper can append/modify); (c) the JSON spec admits these escapes natively, so a script that calls `bm add` programmatically may pass them in.

DESIGN.md § Edge case catalog acknowledges "URL containing newlines: accepted. May visually break the `bm list` output, which is acceptable for this scope." — the acknowledgement is **visual breakage**, not **terminal-escape injection as a security primitive**. The spec is silent on the security implication. The Red Team supplement § cli (not loaded inline here but applicable per [`../../../../vsdd-suite/supplements/cli.md`](../../../../vsdd-suite/supplements/cli.md)) and standard terminal-output hardening guidance (strip C0/C1 control sequences before display; restrict to printable ASCII + UTF-8 from a safe subset) is the standard mitigation.

**Why Raised to SO rather than Resolved or Accepted risk:** the spec is silent on the threat. Either (a) DESIGN.md should explicitly accept the risk with rationale (the user-is-responsible doctrine extended to "and you accept that displaying stored URLs can hijack your terminal"), OR (b) DESIGN.md should add a behavioral contract: `bm list` strips C0/C1 control characters from URL field before rendering, and the implementation follows. Either direction is the Solution Owner's call. Recording as Raised to SO so the spec's silence is closed — the current state (spec doesn't mention it, code doesn't defend) is the worst-of-both-worlds shape.

**Proposed change (for SO review):** Add to DESIGN.md § Behavioral contracts ` PROT_41 ` an explicit clause on stdout safety, e.g., "URLs are emitted as written; the caller is responsible for the safety of stored content (the tool does not strip control characters). Operators displaying bookmarks added from untrusted sources should pipe `bm list` through a sanitizer such as `cat -v` or `tr -d '\\000-\\010\\013-\\037\\177'`." OR alternatively, add: "Before emission, the URL field has C0 and C1 control characters (0x00–0x1F, 0x7F–0x9F excluding `0x09 0x0A 0x0D`) replaced with their `\\xNN` text form." Either route closes the spec-silence; my Red Team recommendation, given the cost asymmetry (one-line `str::chars().filter(...)` in the renderer vs. a real attack primitive), is the latter.

**Classification:** Raised to SO. Proposed DESIGN.md amendment above. Coordination: routes to [SOLUTION-OWNER-REVIEW.md](../SOLUTION-OWNER-REVIEW.md) for spec resolution; the implementation side, once SO directs, routes to [SOFTWARE-ENGINEER-REVIEW.md](../SOFTWARE-ENGINEER-REVIEW.md). Security should have caught this — flag also to [SECURITY-REVIEW.md](../SECURITY-REVIEW.md) per the Red-Team-to-Security flag-back convention in the domain prompt.

---

**Finding 5 — Storage file written with default 0644 (world-readable) leaks URL contents on shared systems (Dim 8 — information leakage; Dim 10 — insider threat)**

**Owner:** solution-owner
**Status:** raised
**Blocked by:** *(none)*

[`src/lib.rs:69`](../../src/lib.rs) calls `std::fs::write(path, json + "\n")`. On Unix, `std::fs::write` creates the file (when absent) with mode `0o666 & !umask` — for the default umask of `022`, that yields mode `0o644` (owner rw, group r, other r). The bookmark store thus becomes readable by every local user. URLs commonly carry sensitive data inline: bearer tokens (`https://example.com/?access_token=...`), OAuth state, basic-auth credentials (`https://user:pass@host`), pre-signed S3 URLs, AWS Console URLs with session tokens, magic-link auth URLs. The file path defaults to `./bookmarks.json` in the CWD, which the user may invoke from any directory (a shared `/srv/`, a project root with a permissive umask, a tmpfs); the README's example `~/.bookmarks.json` is owner-only in a single-user home but a multi-user home (Linux `/home/<user>` with group `users` and `0755` perms) still exposes the file to group/other readers.

**Attack chains this enables:**
1. **Token harvesting on a shared workstation.** User A captures bookmarks containing OAuth callback URLs with `code=` or `access_token=` params. User B on the same host reads `~A/bookmarks.json` (or `/srv/projects/<dir>/bookmarks.json`) and harvests the tokens.
2. **Reconnaissance on multi-tenant hosts.** Even URLs without inline credentials reveal what the user is doing — internal hostnames, project names, auth endpoints. The file is a low-noise enumeration target.

DESIGN.md positions the project as "single-user local tool" but the default CWD-relative path means the file lands wherever the user runs the command from. On a personal laptop, no exposure; on a shared host, hosted dev environment, or any path with permissive perms, real exposure.

**Why Raised to SO rather than fixed:** the spec is silent on file permissions. Either DESIGN.md should add a behavioral contract ("storage file is created with mode 0o600 / owner rw only") and the implementation switches to `OpenOptions::new().mode(0o600).write(true).create(true).truncate(true).open(path)`, OR DESIGN.md should explicitly accept the risk ("storage file inherits default umask; operators on shared hosts must adjust umask before running `bm`"). The current state — spec silent, code writes 0644 — is the worst-of-both-worlds.

**Proposed change (for SO review):** Add to DESIGN.md § Storage format: "The storage file is created with mode `0o600` (owner read-write only) on Unix-like platforms. URLs may contain credentials; the file is treated as user-private." Implementation: replace `std::fs::write` with `OpenOptions::new().mode(0o600).write(true).create(true).truncate(true).open(path)` + `.write_all(...)`. macOS + Linux both support this via `std::os::unix::fs::OpenOptionsExt::mode`; Windows is "untested" per DESIGN.md § Constraints so the `#[cfg(unix)]` gate is acceptable.

**Classification:** Raised to SO. Proposed DESIGN.md amendment above. Coordination: routes to [SOLUTION-OWNER-REVIEW.md](../SOLUTION-OWNER-REVIEW.md); the implementation side routes to [SOFTWARE-ENGINEER-REVIEW.md](../SOFTWARE-ENGINEER-REVIEW.md). Also flag to [SECURITY-REVIEW.md](../SECURITY-REVIEW.md) — Security should have caught this in their first round per the absent-control criterion in the Red Team domain's flag-back convention.

---

**Finding 6 — Symlink-follow on save enables write-target redirection (Dim 6 — server-side attack chains — file confusion; Rust supplement § Red Team — path traversal)**

**Owner:** solution-owner
**Status:** raised
**Blocked by:** *(none)*

[`src/lib.rs:67-70`](../../src/lib.rs) (`save`) calls `std::fs::write(path, ...)` which follows symlinks. [`src/lib.rs:49-50`](../../src/lib.rs) (`load`) calls `std::fs::read_to_string(path)` which also follows symlinks. Neither path uses `O_NOFOLLOW`, `fs::symlink_metadata`, or any `is_symlink` check.

**Attack scenario:** an adversary with write access to the parent directory of the storage path (e.g., `/tmp/`, a shared `/srv/projects/`, or a directory the user just `cd`'d into from an untrusted clone) pre-stages a symlink at the storage path before the user runs `bm`. The symlink target is a file the user has write access to but the attacker does not (e.g., `~/.config/important.json`, a project's `.env`, a CI secret file the user can read+write). When the user runs `bm add <any-url>`:

1. `load()` reads the file via `read_to_string` — follows symlink — reads target content.
2. If target is empty or matches `BookmarkStore` schema (rare; usually triggers parse error and exit 2), proceed; otherwise the user gets `Error: parsing bookmark store...` and exits — but the attacker has now learned something about the target (parse-error vs. empty vs. valid → oracle for content shape).
3. `save()` writes via `fs::write` — follows symlink — overwrites target with serialized JSON.

End state: a file the user has write access to is overwritten with the bookmark JSON, even though the user thinks they're writing only to `bookmarks.json`. The attacker doesn't need write permission on the target — they only need to control the symlink staging.

**Compounding with Finding 1:** an attacker who can both (a) inject `$BOOKMARK_CLI_DB=/<some-path>` AND (b) pre-stage a symlink at `/<some-path>` gets a two-step write primitive — the env var picks the symlink; the symlink redirects to the actual target. This is the Dim 9 chained-vulnerability shape; surfaced here.

**Why Raised to SO rather than implementation-only fix:** the spec is silent on FS hardening. The fix requires a spec-level commitment ("the tool refuses to write through symlinks at the storage path") so the behavioral contract is testable — the implementation change is then `OpenOptions::new().custom_flags(libc::O_NOFOLLOW).write(true).create(true).truncate(true).open(path)` for save, and analogous handling for load. The spec change is the gating step; implementation follows naturally.

**Proposed change (for SO review):** Add to DESIGN.md § Storage format: "The storage path must not be a symlink. If the resolved path is a symlink, the tool errors with `Error: storage path is a symlink; refusing to follow.` and exits 2 — no file is written or read through the symlink." Implementation: pre-flight `fs::symlink_metadata(path)` and reject if `file_type().is_symlink()` for both load and save paths; OR use `O_NOFOLLOW` open via `OpenOptionsExt::custom_flags`. The pre-flight check has a TOCTOU window of its own; `O_NOFOLLOW` is the stronger pattern and the Rust supplement § Red Team — path traversal entry implicitly endorses it ("Use `canonicalize` and verify the result is within the intended root, or reject non-component path segments explicitly" — same family of defenses).

**Classification:** Raised to SO. Proposed DESIGN.md amendment above. Coordination: routes to [SOLUTION-OWNER-REVIEW.md](../SOLUTION-OWNER-REVIEW.md); the implementation side routes to [SOFTWARE-ENGINEER-REVIEW.md](../SOFTWARE-ENGINEER-REVIEW.md). Also flag to [SECURITY-REVIEW.md](../SECURITY-REVIEW.md) — absent-control finding per the Red-Team-to-Security flag-back convention.

---

### Dismissed

**Finding 7 — No `cargo audit` / `cargo deny` in CI (Dim 12 — supply chain)**

**Owner:** platform-engineer
**Status:** raised
**Blocked by:** *(none)*

There is no `.github/workflows/`, no `.cargo/config.toml` audit hook, no `deny.toml`, no scheduled `cargo audit` invocation. The Rust supplement § Red Team — Crates.io supply chain entry calls out `cargo deny check` as the standard tooling; the JSON supplement does not extend the supply-chain dimension. With 90+ transitive dependencies (per `Cargo.lock`, 952 lines), an unmonitored CVE on a transitive (e.g., a future `serde_json` advisory, a `chrono` time-handling CVE, a `clap` parser CVE) would not be detected by the project's own discipline.

**Why Dismissed rather than Raised to SO or Accepted risk:** this is the Platform Engineer's territory, not Red Team's. Red Team's role under Dim 12 is to identify how a vulnerable dependency would actually be exploited in this application's context — without a specific advisory in hand against an in-tree dependency (Cargo.lock shows `chrono 0.4.44`, `serde_json` resolved current, `clap 4.6.1`, `anyhow 1.0.102` — none of these has a public RUSTSEC advisory I can cite without running the tool), the Red Team finding is "the gate is absent," which is more naturally owned by Platform Engineer per [PLATFORM-ENGINEER-REVIEW.md](../PLATFORM-ENGINEER-REVIEW.md) Dim 38 (fresh-system install verification + supply-chain). Dismissed here; coordination flags below.

**Classification:** Dismissed — routed to Platform Engineer's domain per the dimension-ownership boundary. The absence of `cargo audit` IS a defect; this Red Team round notes it but defers ownership rather than dual-classifying. Coordination: flag to [PLATFORM-ENGINEER-REVIEW.md](../PLATFORM-ENGINEER-REVIEW.md) for the audit-gate concern + [SECURITY-REVIEW.md](../SECURITY-REVIEW.md) for the supply-chain-CVE-monitoring shape.

---

### Hallucinated

**Finding 8 — `unsafe` block exploitation (Rust supplement § Red Team)**

The Rust supplement § Red Team entry on `unsafe` block exploitation is one of the canonical attack surfaces for any Rust project. Search of [`src/main.rs`](../../src/main.rs) and [`src/lib.rs`](../../src/lib.rs) for `unsafe` returns no matches — neither file contains an `unsafe` block. Transitive dependencies use `unsafe` (every Rust crate that does FFI or manual memory management does), but the bookmark-cli surface is `unsafe`-free. The attack class does not apply.

**Classification:** Hallucinated. The dimension was enumerated by the supplement; the project's source is `unsafe`-free; the concern is correctly absent. Recorded as Hallucinated (rather than omitted) per primer 3 § Sycophancy check — surfacing the enumerated dimension WITH the verification that the control holds is the discipline; silently skipping it loses the audit signal.

---

**Finding 9 — Integer overflow in release builds (Rust supplement § Red Team)**

The Rust supplement § Red Team entry on integer overflow names release-build silent wrap as a defect class. Search of [`src/main.rs`](../../src/main.rs) and [`src/lib.rs`](../../src/lib.rs) for arithmetic on user-influenced values: the only arithmetic is `b.timestamp.cmp(&a.timestamp)` in `newest_first` ([`src/lib.rs:87`](../../src/lib.rs)) which is a comparison, not an arithmetic operation; `Vec::push` in `add` does not expose its internal length arithmetic. There is no `+`, `-`, `*` on user-input values, no index arithmetic, no offset computation. The defect class does not apply.

**Classification:** Hallucinated. Same shape as Finding 8 — supplement-enumerated, verified absent, recorded for audit-trail completeness.

---

**Finding 10 — Panic-as-DoS via `unwrap()` / `expect()` on user-influenced values (Rust supplement § Red Team)**

The Rust supplement § Red Team entry on panic-as-DoS calls out every `.unwrap()` / `.expect()` on user-influenced values. Audit of non-test code: [`src/main.rs`](../../src/main.rs) and [`src/lib.rs`](../../src/lib.rs) outside `#[cfg(test)]` contain no `.unwrap()` or `.expect()` — every fallible operation propagates via `?` to `anyhow::Result`. The test module ([`src/lib.rs:92-169`](../../src/lib.rs) and [`tests/bookmarks.rs`](../../tests/bookmarks.rs)) uses `.unwrap()` extensively, which is normal for tests and not in the attack surface (tests don't ship with the binary's user-facing surface).

**Classification:** Hallucinated. Verified absent in production code; the panic-as-DoS vector does not apply.

---

**Finding 11 — JSON parse depth-bomb DoS (JSON supplement § Security — depth limit)**

The JSON supplement § Security entry on untrusted-JSON parsing — depth limit calls out stack overflow via deeply-nested JSON. `serde_json` enforces a default recursion limit (128) on `from_str` and rejects deeper inputs with `Error: recursion limit exceeded` (returned as a normal `serde_json::Error`, propagated via `?` to the anyhow context wrapper at [`src/lib.rs:54-55`](../../src/lib.rs)). The attack produces exit code 2 with `Error: parsing bookmark store at <path>` — graceful failure, no stack overflow, no panic.

The string-length-limit and document-size-limit entries (also JSON supplement § Security) are NOT mitigated by serde_json defaults — those are covered by Finding 2 (Accepted risk) and are not Hallucinated. Only the depth-limit attack is Hallucinated for this codebase.

**Classification:** Hallucinated. The control (serde_json's built-in recursion limit) holds. Recorded with specific evidence (the limit is a library-level default, not a project-level control) so a future Red Team round verifying serde_json upgrade does not silently inherit the assumption.

---

### Summary

11 findings filed against bookmark-cli Layer 1 in this first Red Team round: 0 Resolved (no fixes applied in-session; Red Team's role is to surface, not to fix); 3 Accepted risk (Findings 1 + 2 + 3 — env-var ambient authority, unbounded URL length, concurrent-write race — each already named in DESIGN.md as a trust delegation or explicit non-goal; the Red Team's contribution is to surface the adversarial framing so the boundary is auditable); 3 Raised to SO (Findings 4 + 5 + 6 — terminal-escape injection, world-readable storage file, symlink-follow on save — each requires a DESIGN.md amendment because the spec is currently silent on the threat; proposed spec language for each is in the finding body); 1 Dismissed (Finding 7 — `cargo audit` gate, routed to Platform Engineer's dimension boundary); 4 Hallucinated (Findings 8 + 9 + 10 + 11 — `unsafe` blocks, integer overflow, panic-as-DoS, JSON depth bomb — each enumerated from the supplement, verified absent or covered by serde_json defaults, recorded for audit-trail completeness per the sycophancy-check discipline).

Three of the Raised-to-SO findings (terminal-escape injection, world-readable file, symlink-follow) also represent absent controls that the parallel Security round should have caught — they are flagged via Coordination below per the Red-Team-to-Security flag-back convention in the [Red Team domain prompt](../../../../vsdd-suite/domains/role/RED-TEAM-REVIEW.md) § Coordination. If the Security round (when it lands) does not also surface these, the divergence is itself a Security-domain finding for a future round.

The overall pattern (no Resolved in this round; mix of Accepted risk + Raised to SO; multiple absent-control flag-backs to Security) is consistent with a first-pass Red Team round against an implementation that prioritized happy-path correctness over adversarial hardening — appropriate for the project's portfolio-era origin, but capstone-intent (PR 6 promotion) raises the bar. The spec amendments proposed above close the gap between the project's current adversarial posture and the capstone-tier baseline. Per primer 3 § Continue trigger (G-131), a Round 2 is mandatory: this round produced new real findings (multiple Raised to SO entries trigger the SO log entry artifact requirement); Round 2 verifies the SO resolutions hold and looks for adjacent defects the resolutions may have introduced.

**Coordination:** Findings 4 + 5 + 6 (terminal escape; file mode; symlink) route to [SOLUTION-OWNER-REVIEW.md](../SOLUTION-OWNER-REVIEW.md) for spec resolution per the Raised-to-SO discipline, and also flag to [SECURITY-REVIEW.md](../SECURITY-REVIEW.md) per the absent-control flag-back convention — these are controls Security should ensure exist; if the parallel Security round did not surface them, the divergence is its own Security-domain finding. Findings 4 + 5 + 6 implementation routes (post-SO) to [SOFTWARE-ENGINEER-REVIEW.md](../SOFTWARE-ENGINEER-REVIEW.md). Finding 2 (URL length DoS) cross-references [QUALITY-ENGINEER-REVIEW.md](../QUALITY-ENGINEER-REVIEW.md) — a length-bound test would close the implementation-side observable. Finding 7 (cargo audit) routes to [PLATFORM-ENGINEER-REVIEW.md](../PLATFORM-ENGINEER-REVIEW.md) for the gate, and to [SECURITY-REVIEW.md](../SECURITY-REVIEW.md) for the supply-chain-monitoring shape. Findings 8 + 9 + 10 + 11 (Hallucinated) need no coordination — recorded for audit completeness only.

---

## Review 2 — 2026-05-20 21:00Z

**Scope:** Phase 3 IAR Round 2 — cold-context adversarial verification of the Round 1 Red Team fix cycle against bookmark-cli Layer 1 (capstone intent). Verifies the 11 Round 1 findings against current state and looks for new attack surface introduced by the fixes. Read [`DESIGN.md`](../../DESIGN.md) (post-Round-2 amendments — § Threat model + § Storage data classification), [`src/lib.rs`](../../src/lib.rs) (post-fix — `display_safe`, atomic-rename save, mode-0o600 temp file, symlink-rejection on save), [`src/main.rs`](../../src/main.rs) (post-fix — every user-derived value wrapped in `display_safe` before `eprintln!` / `println!`), [`tests/bookmarks.rs`](../../tests/bookmarks.rs) (post-fix integration tests), [`deny.toml`](../../deny.toml) (Round 2 artifact), [`.github/workflows/bookmark-cli-manual.yml`](../../../../.github/workflows/bookmark-cli-manual.yml) (Round 2 artifact — `audit` + `deny` CI jobs), [Red Team Review 1](2026-05-20-red-team.md), [Security Review 1](2026-05-20-security.md). Applied the [Red Team domain](../../../../vsdd-suite/domains/role/RED-TEAM-REVIEW.md) dimensions + the [Rust supplement](../../../../vsdd-suite/supplements/rust.md) § Red Team + [JSON supplement](../../../../vsdd-suite/supplements/json.md) § Security as the floor.

**Lens:** Adversarial cold-context Round 2. Posture per the [Phase 3 primer](../../../../vsdd-suite/primers/3-review-session.md): regression-check every Round-1 control + adversarially probe each new control for bypass. The continue trigger (G-131) fired Round 1 → mandatory Round 2; the round-pre-check is "do the fixes hold, and what adjacent defects did the fixes introduce?"

**Session note:** Cold-context session — this reviewer did not author the Round 2 fix cycle nor any prior artifact. Sycophancy-compensation: explicitly attempted to bypass each new control before classifying it Resolved — symlink check probed for TOCTOU windows (race between `symlink_metadata` and `rename`); `display_safe` probed for Cf gaps outside the spec's named ranges (U+061C, tag characters, variation selectors); mode-0600 probed for the temp-file pre-rename window; atomic-rename probed for predictable-temp-path symlink staging. The bypass attempts are documented per-finding rather than asserted-and-skipped.

**Source:** `domain-raised` — every Finding below was elicited by applying the Red Team domain dimensions + the Rust + JSON supplement floor against the post-fix source. No director-raised observations interrupted the round.

**Posture:** Adversarial. Threat actors unchanged from Round 1: (a) co-tenant on shared Unix host with FS-read access to the parent dir; (b) hostile script the user pastes that calls `bm add <attacker-URL>` or pre-stages files at the storage path; (c) polluted shell env that sets `$BOOKMARK_CLI_DB`; (d) co-resident process racing the user's `bm add`. Crown jewel unchanged: integrity of the bookmark file + the user's terminal session.

**Regression check (Round 1 controls):** every Round 1 finding's post-fix state was re-verified against current source. Results per finding below. The Round 1 Raised-to-SO cluster (F4 + F5 + F6) was routed through SO and landed as a single Round 2 fix-cycle that produced four artifacts: the `display_safe` sanitizer + every `eprintln!` / `println!` interpolation wrapped (F4); `OpenOptions::mode(0o600)` on temp file + DESIGN.md § Storage data classification declaring confidential tier (F5); `symlink_metadata` + symlink rejection on save + DESIGN.md § Threat model entry (F6); `deny.toml` + `.github/workflows/bookmark-cli-manual.yml` `audit` and `deny` jobs (F7).

---

### Accepted risk

**Finding 1 — Round 1 F4 terminal-escape injection via stored URL — verify `display_safe` sanitizer holds and is universally applied (Dim 7)**

<a id="r2-f1"></a>

**Owner:** red-team
**Status:** validated
**Blocked by:** *(none)*
**Validator:** security

Round 1 F4 raised terminal-escape injection via `println!("{} {}", ts, url)` rendering of stored URLs containing ANSI escapes, OSC sequences, bidi format chars, and zero-width controls. Round 2 fix landed: [`src/lib.rs:262-308`](../../src/lib.rs) defines `display_safe(s: &str) -> String` which escapes `is_control()` chars + a named subset of Cf format chars (U+200B–U+200F, U+202A–U+202E, U+2066–U+2069, U+FEFF) as `\u{HHHH}` while preserving `\n` and `\t`. [`src/main.rs:125`](../../src/main.rs) wraps the URL render: `println!("{} {}", bm.timestamp().to_rfc3339(), display_safe(bm.url()))`. Every `eprintln!` site for a user-derived value is also wrapped: lines 95, 103, 107, 116 all use `display_safe(&format!("{e:#}"))` so the anyhow chain (which interpolates `path.display()` via `with_context`) is sanitized at the emission boundary.

**Adversarial bypass attempts:**

1. **ANSI ESC (U+001B):** `is_control() == true` → escaped to `\u{001b}`. Integration test [`tests/bookmarks.rs:317-351`](../../tests/bookmarks.rs) (`bm_list_sanitizes_terminal_escape_in_url`) asserts `rendered.contains("\\u{001b}")` AND `!rendered.contains('\x1b')`. The raw byte cannot survive. **Bypass fails.**
2. **OSC 8 hyperlink masquerade (`\x1b]8;;URL\x07text\x1b]8;;\x07`):** Every byte is either ESC (Cc) or BEL (0x07, Cc) — both `is_control()`. All escaped. **Bypass fails.**
3. **Bidi U+202E (RLO):** Matched by `is_format_char` per [`src/lib.rs:303-308`](../../src/lib.rs). Unit test [`src/lib.rs:445-452`](../../src/lib.rs) (`display_safe_escapes_format_chars`) asserts the RLO is escaped. **Bypass fails.**
4. **Zero-width chars U+200B / U+200C / U+200D / U+200E / U+200F:** All in the 0x200B–0x200F matched range. Escaped. **Bypass fails.**
5. **Cursor-up + erase-line spoofing (`\x1b[F\x1b[K`):** All bytes are Cc; escaped. **Bypass fails.**
6. **Newlines preserved as legitimate whitespace:** Unit test [`src/lib.rs:426-429`](../../src/lib.rs) asserts `\n` and `\t` pass through. Multi-line URLs render as multi-line output — consistent with the spec's "URL containing newlines: accepted" edge case + does not undermine the sanitizer's escape discipline.

The control is structurally sound for the threat model named in [`DESIGN.md`](../../DESIGN.md) § Threat model. The named Cf subset matches the threat model's explicit enumeration; broader Cf coverage is the subject of Finding 4 below (a hardening recommendation, not a control bypass).

**Resolution:** Round 1 F4 fix verified. `display_safe` covers every adversary-controlled value reaching the terminal; the integration test plus three unit tests assert the control under three distinct shapes (ANSI, RLO, whitespace preservation). The validator pair (Security ↔ Red Team) confirms the Dim 9 / Rust supplement § Security floor holds (Dim 7)

---

**Finding 2 — Round 1 F5 world-readable storage file — verify mode 0o600 holds and the temp-file pre-rename window is also 0o600 (Dim 8)**

<a id="r2-f2"></a>

**Owner:** red-team
**Status:** validated
**Blocked by:** *(none)*
**Validator:** security

Round 1 F5 raised the default-umask `0o644` exposure of confidential-class data (browsing-history URLs that commonly contain bearer tokens, OAuth state, basic-auth creds). Round 2 fix landed: [`src/lib.rs:234-247`](../../src/lib.rs) defines a `#[cfg(unix)]` `write_temp_file` that opens with `OpenOptions::new().write(true).create_new(true).mode(0o600).open(tmp_path)?`. The temp file is owner-only from creation; the subsequent `rename(tmp_path, path)` preserves the inode's permissions, so the on-disk file ends up at mode `0o600`. DESIGN.md § Storage data classification declares the confidential tier explicitly ([`DESIGN.md:194-198`](../../DESIGN.md)).

**Adversarial bypass attempts:**

1. **Pre-rename window — is the temp file ever world-readable?** The `create_new(true).mode(0o600)` call passes `O_CREAT | O_EXCL` with mode `0o600` to `open(2)`. The kernel applies `actual = mode & ~umask` at file-creation time — so with umask `0o022`, actual = `0o600 & ~0o022 = 0o600`. With umask `0o000`, actual = `0o600`. With umask `0o077`, actual = `0o600 & ~0o077 = 0o600`. The temp file is owner-only at creation; no window during which it is world-readable. **Bypass fails.**
2. **Umask edge case — what if operator's umask is `0o600`?** actual = `0o600 & ~0o600 = 0o000`. The file would have no permissions and even the owner couldn't read it back. This is a pathological operator-controlled umask; not exploitable by an attacker (the attacker can't set the user's umask without already controlling the shell). The integration test at [`tests/bookmarks.rs:294-311`](../../tests/bookmarks.rs) (`save_creates_file_with_mode_0600_on_unix`) runs under the test process's umask (typically `0o022`) and asserts mode `0o600`; the test would fail under a `0o600`-umask environment but that environment is outside the threat model.
3. **TOCTOU between temp creation and rename — can attacker substitute the temp file?** The temp file is opened with `O_EXCL` ([`create_new(true)`](../../src/lib.rs)) which atomically creates-or-fails. The attacker cannot replace it without an unlink first. Unlinking the temp file would cause `f.write_all` or `f.sync_all` to fail (write to deleted inode succeeds against the still-open fd; sync_all may surface the issue depending on filesystem). Even if the attacker unlinks + recreates at the temp path, the `rename(tmp_path, path)` is by path, so the rename would move the attacker's file. **But** — the attacker would need write access to the parent directory the user controls, at which point they already have F1's accepted-risk env-var primitive. No new attack capability is granted.
4. **The Round 1 finding included `~/.bookmarks.json` under default umask** — verified the integration test catches a regression that re-introduces `std::fs::write`. The mode assertion is exact (`assert_eq!(mode, 0o600, "expected mode 0600, got {mode:o}")`) and would fail loudly on regression.

The unit test at [`src/lib.rs:483-494`](../../src/lib.rs) (`save_creates_file_with_mode_0600`) provides a second-source verification via the library API directly.

**Resolution:** Round 1 F5 fix verified. Mode `0o600` is set at temp-file creation; no world-readable window exists pre-rename; DESIGN.md § Storage data classification declares the confidential tier so the spec contract is testable. The validator pair confirms the Dim 8 floor holds (Dim 8)

---

**Finding 3 — Round 1 F6 symlink-follow on save — verify `symlink_metadata` rejection holds against the save path (Dim 6)**

<a id="r2-f3"></a>

**Owner:** red-team
**Status:** validated
**Blocked by:** *(none)*
**Validator:** security

Round 1 F6 raised symlink-follow attack on both load and save paths. Round 2 fix on save: [`src/lib.rs:131-139`](../../src/lib.rs) calls `std::fs::symlink_metadata(path)` and refuses with an explicit `anyhow!("refusing to write through symlink at {}: ...")` if `file_type().is_symlink()` returns true. DESIGN.md § Threat model declares the discipline ([`DESIGN.md:184`](../../DESIGN.md)): "symlink-follow-rejection on save (the implementation checks `symlink_metadata` + refuses to write through a symlink pointing outside the parent directory)".

**Adversarial bypass attempts:**

1. **TOCTOU race between `symlink_metadata(path)` and `rename(tmp_path, path)` — can attacker create a symlink at `path` after the check passes?** This was explicitly probed. POSIX `rename(2)` semantics: when the destination of `rename(src, dst)` exists, the destination is replaced atomically — and crucially **`rename(2)` does NOT follow symlinks at the destination**. If an attacker wins the TOCTOU race and creates a symlink at `path` between the check and the rename, the rename replaces the symlink itself with the temp file's contents; the symlink's actual target is untouched. The window exists but is not exploitable for write-redirection. **Bypass fails.**
2. **Path-of-creation race — what if `path` doesn't exist at check time?** The `if let Ok(meta) = std::fs::symlink_metadata(path)` arm at [`src/lib.rs:132`](../../src/lib.rs) silently passes through when `path` is absent (the `Err(NotFound)` branch is the no-op). An attacker who creates a symlink at `path` between this point and the rename runs into the same `rename(2)`-replaces-symlink semantics analyzed above. **Bypass fails.**
3. **Symlinked parent component** — `symlink_metadata(path)` only checks if the final-component basename is a symlink; intermediate path components may be symlinks and are followed. E.g., `path = /home/user/data/bookmarks.json` where `/home/user/data` is a symlink to an attacker-controlled directory. The save writes into the attacker's directory. **However** — this is consistent with the spec: DESIGN.md § Threat model names the discipline as "the storage path must not be a symlink" (the path itself, not its parent). The env-var ambient-authority finding (Round 1 F1, Accepted risk) already documents that the user is responsible for the resolved path including its parent chain. **Not a bypass of the named control; consistent with F1's accepted boundary.**
4. **Symlink-to-directory scenario** — `path` is a symlink pointing at a directory. `symlink_metadata` returns `file_type().is_symlink() == true` → rejected. **Bypass fails.**

The integration test at [`tests/bookmarks.rs:255-287`](../../tests/bookmarks.rs) (`save_refuses_symlink_target`) pre-stages a symlink at `BOOKMARK_CLI_DB`, runs `bm add`, asserts exit 2 + stderr contains "refusing to write through symlink" + the symlink's target file is byte-identical to its pre-test contents. The unit test at [`src/lib.rs:455-481`](../../src/lib.rs) (`save_refuses_symlink`) provides second-source verification via the library API.

**Resolution:** Round 1 F6 save-path fix verified. The named threat (write-target redirection via symlink at the storage path) is closed by the `symlink_metadata` pre-check + `rename(2)` semantics. **However** — the Round 1 finding's full text named BOTH `load()` and `save()` as needing the fix; the Round 2 implementation closed save only. The load-side symlink-follow surface is the subject of Finding 5 below (Open) — the validator pair convention surfaces the asymmetry as a new finding rather than degrading this Resolved classification (Dim 6)

---

**Finding 4 — Round 1 F7 absence of `cargo audit` / `cargo deny` — verify `deny.toml` + CI workflow audit job hold (Dim 12)**

<a id="r2-f4"></a>

**Owner:** red-team
**Status:** validated
**Blocked by:** *(none)*
**Validator:** security

Round 1 F7 raised the absence of supply-chain audit tooling. Round 2 fix landed two artifacts: [`deny.toml`](../../deny.toml) at the project root with all four required sections (`[advisories]` / `[licenses]` / `[bans]` / `[sources]`) per the Rust supplement § PE, and [`.github/workflows/bookmark-cli-manual.yml`](../../../../.github/workflows/bookmark-cli-manual.yml) with two parallel jobs — `audit` (lines 102-121: `cargo install cargo-audit --locked --version 0.22.1` + `cargo audit`) and `deny` (lines 82-100: `cargo install cargo-deny --locked --version 0.19.4` + `cargo deny --locked check`).

**Adversarial bypass attempts:**

1. **`deny.toml` completeness — do all four sections gate?** `[advisories]` declares `version = 2` + `yanked = "deny"` + empty `ignore` list (no exemptions) — known-CVE gate active. `[licenses]` declares `version = 2` + an explicit allowlist (`MIT`, `Apache-2.0`, `BSD-2-Clause`, `BSD-3-Clause`, `ISC`, `Unicode-DFS-2016`, `Unicode-3.0`, plus the `Apache-2.0 WITH LLVM-exception` exception) — license-policy gate active. `[bans]` declares `multiple-versions = "warn"` + `wildcards = "deny"` — duplicate-version and wildcard-version gates active. `[sources]` declares `unknown-registry = "deny"` + `unknown-git = "deny"` + `allow-registry = ["https://github.com/rust-lang/crates.io-index"]` + `allow-git = []` — source-policy gate active, mechanizing the DESIGN.md § Constraints "all from crates.io, no git deps" commitment. **All four sections gate; bypass via configuration gap fails.**
2. **CI trigger surface — is the audit job actually run on every push?** The workflow's `on:` block triggers on `push: branches: ['**']` (every branch, every push) + `pull_request: branches: [main]`, with `paths: vsdd-suite-reference-examples/bookmark-cli-manual/**` filter. A PR modifying `Cargo.toml` or `Cargo.lock` (which sit under that path) triggers the audit job. A push directly to `main` also triggers. **Bypass via "PR avoids the gate" fails.**
3. **Tool version pinning** — `cargo-audit` pinned to `0.22.1`; `cargo-deny` pinned to `0.19.4`. A future supply-chain attacker compromising a tool author's account cannot inject a malicious tool version via floating semver. **Bypass via tool-supply-chain fails.**
4. **Action pinning** — the workflow uses `actions/checkout@v4` and `dtolnay/rust-toolchain@stable` (tag-based, not SHA-pinned). Tag-pinning is weaker than SHA-pinning (a tag can be moved by the action author). The sibling `issue-tracker-cli.yml` uses SHA-pinning per its PE R8 closure; bookmark-cli-manual uses tag-pinning. This is a PE-domain concern (action-pinning discipline) rather than a Red Team bypass — the audit job itself runs `cargo audit` against `Cargo.lock`, which is the relevant control. Flagged for cross-domain coordination but not a Red Team finding.

**Resolution:** Round 1 F7 fix verified. The two-tool supply-chain gate (`cargo audit` + `cargo deny check`) runs on every PR and every push. The action-pinning concern is routed to [Platform Engineer](../PLATFORM-ENGINEER-REVIEW.md) per the dimension-ownership boundary established in Round 1 F7 (Dim 12)

---

### Resolved

**Finding 5 — `BookmarkStore::load` still follows symlinks; Round 1 F6 save-side fix did not close the load-side surface (Dim 6 — server-side attack chains)**

<a id="r2-f5"></a>

**Owner:** security
**Status:** raised
**Blocked by:** *(none)*
**Validator:** security

[`src/lib.rs:86-97`](../../src/lib.rs) (`BookmarkStore::load`) calls `path.exists()` then `std::fs::read_to_string(path)`. Neither call rejects symlinks — `read_to_string` follows symlinks transparently. Round 1 F6 explicitly named the load path as needing the fix (Round 1 F6 reproducer step 1: "`load()` reads the file via `read_to_string` — follows symlink — reads target content"). The Round 2 fix added symlink-rejection on save only ([`src/lib.rs:131-139`](../../src/lib.rs)); the same discipline is absent from load.

**Attack scenario (verified against current source):**

1. Attacker with write access to the parent directory of the storage path (e.g., `/tmp/`, or a directory the user just `cd`'d into from an untrusted clone) pre-stages a symlink at `bookmarks.json` pointing at a file the user has read access to (e.g., `~/.ssh/known_hosts`, `~/.config/credentials.json`, a project's `.env`).
2. User runs `bm list` or `bm add <url>`. `BookmarkStore::load` calls `path.exists()` → true (the symlink exists). `read_to_string` follows the symlink → reads the target file's content.
3. Target is parsed as JSON. Three observable outcomes give the attacker an oracle:
   - **Valid `BookmarkStore` JSON** (rare; attacker has staged a deliberately-crafted target) → bookmarks render via `display_safe` (no escape-injection — F1 above closes that), but the attacker confirms the target file matches the schema.
   - **Invalid JSON** → error to stderr exit 2: `Error: parsing bookmark store at <path>: ...`. The error message via `display_safe` is escape-safe, but the parse-error existence reveals "the file is non-empty and contains data". For attacker probing `~/.ssh/known_hosts`, this confirms the file exists and is non-empty.
   - **Empty file** → silent empty-bookmark state. Reveals "target file is empty / zero bytes".

The oracle is weaker than a full read primitive (the attacker doesn't receive file contents directly), but it confirms file existence + non-emptiness + schema-shape via three observable channels (parse-error vs. empty vs. valid). For sensitive-file enumeration on shared hosts, this is a meaningful primitive.

**Critically:** the `bm add` flow compounds. After load, the in-memory `BookmarkStore` (containing whatever was parsed from the symlink target, if it happened to be valid JSON) gets a new bookmark appended via `add()` and saved via `save()`. `save()` rejects the symlink at the storage path → exit 2 → no overwrite. **The save-side fix prevents the write-redirection primitive,** but the load-side read-oracle primitive remains.

**Why this was missed in Round 2:** the Round 1 F6 finding body conflated load and save into one finding ("`load()` reads the file via `read_to_string` — follows symlink — reads target content. ... `save()` writes via `fs::write` — follows symlink — overwrites target."). The Round 2 fix implemented the save-side discipline and updated DESIGN.md § Threat model to declare "symlink-follow-rejection on save" — the spec amendment is narrower than the finding it closed. The integration test at [`tests/bookmarks.rs:255-287`](../../tests/bookmarks.rs) tests save only; no test exercises the load path against a symlink.

**Proposed fix:** mirror the `symlink_metadata` check in `BookmarkStore::load` — if `path.exists()` succeeds AND `symlink_metadata(path).file_type().is_symlink()` returns true, refuse with the same error shape ("refusing to read through symlink at <path>"). Update DESIGN.md § Threat model to extend the discipline: "symlink-follow-rejection on both load AND save". Add an integration test pre-staging a symlink and running `bm list` → assert exit 2 + stderr contains "refusing to read through symlink".

**Classification:** Open — the surface is structurally real and the Round 1 finding explicitly named it; the Round 2 fix closed half of it. Routed to `software-engineer` for the implementation + `solution-owner` for the DESIGN.md § Threat model extension. Red Team validates the post-fix oracle is closed (Dim 6)

---

**Finding 6 — `display_safe` Cf coverage narrower than Rust supplement names; tag characters and U+061C bypass the named-subset matcher (Dim 7 — client-side attack chains)**

<a id="r2-f6"></a>

**Owner:** security
**Status:** raised
**Blocked by:** *(none)*
**Validator:** security

[`src/lib.rs:303-308`](../../src/lib.rs) defines `is_format_char` as:

```rust
const fn is_format_char(c: char) -> bool {
    matches!(
        c as u32,
        0x200B..=0x200F | 0x202A..=0x202E | 0x2066..=0x2069 | 0xFEFF
    )
}
```

The named ranges cover zero-width chars + standard bidi controls + BOM. The Rust supplement § Security states the sanitizer "must escape `is_control()` chars (`Cc`) AND `Cf` format chars (Trojan-Source U+202E and zero-width characters)". The supplement's phrasing names "Cf format chars" as a category; the named examples (U+202E, zero-width) are illustrative, not exhaustive. Several Unicode Cf chars outside the matched ranges flow through `display_safe` unescaped:

1. **U+061C ARABIC LETTER MARK (ALM):** added Unicode 6.3. A bidi format char that strongly influences bidi-context rendering in mixed Arabic/Latin contexts. Not in `is_format_char`'s matched ranges; `is_control()` returns false. **Bypasses sanitization.** A URL containing `https://example.com/\u{061c}path` renders raw to the terminal — most modern terminals (kitty, iTerm2, gnome-terminal recent) honor ALM and reflow the surrounding text accordingly. Visual-spoofing-via-bidi is reduced but not eliminated.
2. **Tag characters U+E0001 + U+E0020–U+E007F:** these are Unicode "tag" characters historically used for language tagging, repurposed by the [Trojan Source paper](https://trojansource.codes/) (2021) as an *invisible* code-injection channel — they encode ASCII text invisibly in supplementary-plane chars that some text-shaping engines pass through. Not in any matched range; `is_control()` returns false. **Bypasses sanitization.** A URL containing tag-character payload renders invisibly in many terminals.
3. **Mongolian Free Variation Selectors U+180B–U+180D:** Cf chars used in Mongolian text shaping. Not matched. **Bypasses sanitization.** Marginal practical impact for URL rendering but a Cf-category gap.
4. **Variation Selectors U+FE00–U+FE0F + U+E0100–U+E01EF:** Cf-family selectors. Not matched. **Bypasses sanitization.** Used for emoji + CJK variation; minor practical attack surface but a Cf-category gap.

The supplement's "Cf format chars" framing is broader than the implementation. The DESIGN.md § Threat model amendment names the matched set as authoritative ("escapes `is_control()` (Cc) chars + `Cf` format chars while preserving `\n` `\t` for legitimate whitespace") — but the spec text says "Cf format chars" without naming the narrowed subset. The spec is silent on whether the narrowing is intentional. **This is a real gap between spec wording and implementation, and the gap admits a real (if narrow) attack class.**

**Sycophancy check applied:** the easy dismissal is "the threat model named the set explicitly in the implementation; the spec text 'Cf format chars' is shorthand for the named set". But the spec text reads naturally as the full Cf category; a future Phase 5 / Phase 6 reviewer or a downstream consumer would not infer the narrowing from DESIGN.md § Threat model alone. The implementation is a strict subset of what the spec promises.

**Two resolution paths:**

(a) **Broaden the implementation** to match the spec wording — use `unicode-general-category` or hand-roll the full Cf range. Adds a dependency (or a larger `matches!` arm) and broadens the escape set, which is the Red Team-preferred direction (closes the named gap; the spec's "Cf format chars" framing is preserved).

(b) **Narrow the spec wording** to match the implementation — amend DESIGN.md § Threat model to enumerate the named subset explicitly: "escapes `is_control()` (Cc) chars + the Cf format chars at U+200B–U+200F, U+202A–U+202E, U+2066–U+2069, U+FEFF, while preserving `\n` `\t` for legitimate whitespace". The spec/code parity is restored; the U+061C + tag-character + variation-selector attack surface becomes documented Accepted risk (the threat model declares "we only defend the named-subset; broader Cf is operator-responsible content").

The Red Team recommendation is (a) for the same reason Round 1 raised the original finding: the cost asymmetry (one-time `unicode-general-category` dep cost vs. a recurring class of bypass) favors broadening the control. The Solution Owner has the call.

**Classification:** Open — Raised to SO for the spec-vs-implementation gap adjudication (which direction: broaden or narrow?). Routed to `solution-owner` for the spec direction + `software-engineer` for the implementation side. Red Team validates the post-fix Cf coverage either matches the narrowed spec or covers the full Cf category (Dim 7)

---

### Accepted risk

**Finding 7 — Round 1 F1 `$BOOKMARK_CLI_DB` ambient authority — verify DESIGN.md § Threat model documents the trust delegation (Dim 5)**

<a id="r2-f7"></a>

**Owner:** solution-owner
**Status:** validated
**Blocked by:** *(none)*
**Validator:** security

Round 1 F1 classified Accepted risk pending the spec-level documentation of the trust delegation. Round 2 verification: [`DESIGN.md:181-186`](../../DESIGN.md) § Threat model § In-scope adversaries enumerates "Adversary-controlled `$BOOKMARK_CLI_DB`" as the named adversary with mitigation = "symlink-follow-rejection on save ... the env var is the user's own shell + the user is responsible for what they set". The trust delegation is now explicit in the spec; the env var IS the operator's trust surface, and the symlink-rejection control covers the most concerning bypass (write-redirection via symlink-at-resolved-path).

**Compounded with Finding 5 above:** the env-var-plus-symlink chain that Round 1 F1 cross-referenced (Dim 9 chained vulnerability — env var picks the symlink; symlink redirects the write) is half-closed by the save-side fix. The load-side oracle (Finding 5) still admits the chain — an attacker who can inject `$BOOKMARK_CLI_DB=/<some-path>` AND pre-stage a symlink at `/<some-path>` gets a two-step **read oracle** primitive (the env var picks the symlink; the symlink redirects the read in `load()`). The save-side cannot exploit; the load-side can. This is structurally Finding 5's surface; the cross-reference is recorded here.

**Classification:** Accepted risk — Round 1 F1 spec-documentation requirement verified; the trust delegation is explicit in DESIGN.md § Threat model. Risk owner: solution-owner. The compound-with-symlink read-oracle is the subject of Finding 5, not a new acceptance (Dim 5)

---

**Finding 8 — Round 1 F2 unbounded URL length — verify DESIGN.md acknowledges the DoS primitive (Dim 11)**

<a id="r2-f8"></a>

**Owner:** solution-owner
**Status:** validated
**Blocked by:** *(none)*
**Validator:** security

Round 1 F2 classified Accepted risk. Round 2 verification: [`DESIGN.md:190`](../../DESIGN.md) § Threat model § Out-of-scope adversaries lists "Unbounded URL length — the spec accepts arbitrarily long URLs per the original § Edge case catalog. DoS-via-memory is acknowledged but accepted at Layer 1 ([Red Team Review 1 Finding 2](2026-05-20-red-team.md) Accepted risk)". The original [`DESIGN.md:80`](../../DESIGN.md) § Edge case catalog entry ("Very long URL (10K+ chars): accepted. No length cap.") remains as the source for the design choice; the threat-model amendment surfaces the DoS framing. Spec acknowledgement complete.

**Classification:** Accepted risk — Round 1 F2 spec-documentation requirement verified; the DoS-via-URL-length primitive is named in DESIGN.md § Threat model as accepted out-of-scope. Risk owner: solution-owner (Dim 11)

---

**Finding 9 — Round 1 F3 concurrent-write race — verify DESIGN.md flags out-of-scope (Dim 5)**

<a id="r2-f9"></a>

**Owner:** solution-owner
**Status:** validated
**Blocked by:** *(none)*
**Validator:** security

Round 1 F3 classified Accepted risk. Round 2 verification: [`DESIGN.md:189`](../../DESIGN.md) § Threat model § Out-of-scope adversaries lists "Same-user concurrent process writing the storage file at the same time — the project is a single-user single-process tool per § Scope and non-goals; concurrent-write race is accepted risk". The original [`DESIGN.md:79`](../../DESIGN.md) § Edge case catalog entry ("Concurrent writes: out of scope; not a multi-process tool. Single user, single shell session.") remains; the threat-model section surfaces the adversarial framing. Atomic save (Round 2 SE Finding 2 fix — temp file + rename) provides crash-during-write protection but does NOT provide concurrent-write protection (two concurrent saves still race: both load state S, both write S+their-add, the second write wins). The Round 1 finding's silent-loss primitive remains, accepted per spec.

**Adversarial probe:** the new atomic-save discipline does not introduce a new concurrent-write surface — the prior `std::fs::write`-based save was equally vulnerable to silent-loss races. The temp-file mechanism is per-process (`pid` + `nanos` in the path → unique per process) so the two concurrent saves write to distinct temp paths, and the two `rename(tmp_a, path)` + `rename(tmp_b, path)` operations are individually atomic — but the last writer wins, losing one bookmark silently. Same shape as Round 1; no new attack surface.

**Classification:** Accepted risk — Round 1 F3 spec-documentation requirement verified; concurrent-write race remains accepted out-of-scope, and the atomic-save discipline does not change the acceptance boundary. Risk owner: solution-owner (Dim 5)

---

### Hallucinated

**Finding 10 — Round 1 F8 unsafe block exploitation — verify still absent (Rust supplement § Red Team)**

<a id="r2-f10"></a>

**Owner:** red-team
**Status:** raised
**Blocked by:** *(none)*
**Validator:** sanity-check

Round 1 F8 verified the project's source was `unsafe`-free. Round 2 verification: `grep -n "unsafe" src/lib.rs src/main.rs` returns matches only at the deny-attribute level ([`src/lib.rs:26`](../../src/lib.rs) `#![deny(missing_docs, unsafe_code)]` and [`src/main.rs:18`](../../src/main.rs) `#![deny(unsafe_code)]`) — these are lint denies, not unsafe blocks. The crate-level deny ensures any future contributor adding an `unsafe` block fails compilation. The Round 2 fixes (atomic save, symlink check, `display_safe`, mode 0o600) added no `unsafe` blocks — verified by re-reading the full source. The attack class continues to not apply.

**Classification:** Hallucinated. The control (no `unsafe`, lint-enforced) holds with stronger evidence than Round 1 (the `#![deny(unsafe_code)]` attribute mechanizes the guarantee). Recorded per the sycophancy-check discipline — surfacing the dimension WITH verification is the audit signal (Rust supplement § Red Team — unsafe block exploitation)

---

**Finding 11 — Round 1 F9 integer overflow in release builds — verify still absent (Rust supplement § Red Team)**

<a id="r2-f11"></a>

**Owner:** red-team
**Status:** raised
**Blocked by:** *(none)*
**Validator:** sanity-check

Round 1 F9 verified no arithmetic on user-influenced values exists. Round 2 verification: re-scanned [`src/lib.rs`](../../src/lib.rs) and [`src/main.rs`](../../src/main.rs) for `+`, `-`, `*`, `/`, `<<`, `>>` on user-influenced values. The new Round 2 code introduces:

1. [`src/lib.rs:220-224`](../../src/lib.rs) `temp_sibling_path`: `SystemTime::now().duration_since(UNIX_EPOCH).map_or(0, |d| d.as_nanos())` returns `u128` nanos — no arithmetic, just a clock read.
2. [`src/lib.rs:224`](../../src/lib.rs) `std::process::id()` returns `u32` pid — no arithmetic.
3. [`src/lib.rs:230`](../../src/lib.rs) `format!(".tmp.{pid}.{nanos}")` — string formatting, no overflow risk.

No user-input arithmetic introduced. The defect class continues to not apply.

**Classification:** Hallucinated. The Round 2 fix-cycle added no integer-arithmetic surface against user input. Recorded for audit completeness (Rust supplement § Red Team — integer overflow)

---

**Finding 12 — Round 1 F10 panic-as-DoS via `unwrap()` / `expect()` — verify still absent in production code (Rust supplement § Red Team)**

<a id="r2-f12"></a>

**Owner:** red-team
**Status:** raised
**Blocked by:** *(none)*
**Validator:** sanity-check

Round 1 F10 verified no `.unwrap()` / `.expect()` exists on user-influenced values outside `#[cfg(test)]`. Round 2 verification: re-scanned the post-fix source. Production-path matches:

1. [`src/lib.rs:223`](../../src/lib.rs) `SystemTime::now().duration_since(UNIX_EPOCH).map_or(0, |d| d.as_nanos())` — uses `map_or` with a fallback `0`, not unwrap. Safe.
2. [`src/lib.rs:225-228`](../../src/lib.rs) `path.file_name().map_or_else(|| OsString::from("bookmarks.json"), OsStr::to_os_string)` — `map_or_else` with a fallback. Safe.
3. Every `?` operator propagates errors via `anyhow::Result`; no `.unwrap()` in production path.

Test-code matches: extensive, expected. Not in the production attack surface.

The Round 2 fix-cycle's two new surfaces (atomic save with rename; symlink check) both propagate errors via `?` and `anyhow!` — no `.unwrap()` was introduced. The defect class continues to not apply.

**Classification:** Hallucinated. Production code remains `.unwrap()`-free; the deny-attribute discipline ([`src/lib.rs:26`](../../src/lib.rs) `#![deny(missing_docs, unsafe_code)]` + the crate-level `#![warn(clippy::all, clippy::pedantic, clippy::nursery)]`) does NOT include `clippy::unwrap_used` — a hardening opportunity flagged for [Software Engineer](../SOFTWARE-ENGINEER-REVIEW.md), not a Red Team finding. Recorded for audit completeness (Rust supplement § Red Team — panic as DoS)

---

**Finding 13 — Round 1 F11 JSON parse depth-bomb DoS — verify serde_json depth limit still holds (JSON supplement § Security)**

<a id="r2-f13"></a>

**Owner:** red-team
**Status:** raised
**Blocked by:** *(none)*
**Validator:** sanity-check

Round 1 F11 verified `serde_json`'s default 128-level recursion limit catches depth-bomb attacks gracefully (parse error, not stack overflow). Round 2 verification: [`Cargo.lock`](../../Cargo.lock) was not modified by the Round 2 fix cycle (the SE/QE/Security/RT fix cycle added no dependencies — atomic-save uses `std::fs` + `std::os::unix::fs::OpenOptionsExt`; `display_safe` is a pure stdlib `String` walk). `serde_json` is at the same resolved version. The depth-limit control holds.

DESIGN.md § Threat model § Out-of-scope adversaries names the protection explicitly ([`DESIGN.md:192`](../../DESIGN.md)): "JSON-parser depth-bomb (deeply-nested user-controlled JSON) — `serde_json` enforces a 128-level recursion limit by default; the attacker model does not grant write access to the store file". The spec amendment formalizes the audit signal.

**Classification:** Hallucinated. The library-level control holds (verified unchanged in `Cargo.lock`); the spec amendment surfaces the protection in DESIGN.md for future-Round audit-trail continuity. Recorded per the sycophancy-check discipline (JSON supplement § Security — depth limit)

---

### Summary

13 findings filed in Round 2 against bookmark-cli Layer 1 post-Round-1-fix-cycle: **4 Resolved** (Findings 1 + 2 + 3 + 4 — `display_safe` sanitizer holds against the named adversarial attack vectors; mode 0o600 holds with no pre-rename window; symlink-rejection on save holds against TOCTOU probes; `deny.toml` + CI `audit` and `deny` jobs gate the supply-chain surface); **2 Open** (Findings 5 + 6 — load-side symlink-follow surface remains [Round 1 F6 closed save only; load was named in the finding text but not in the spec amendment]; `display_safe` Cf coverage narrower than Rust supplement names [U+061C + tag chars + Mongolian + variation selectors bypass the named-subset matcher]); **3 Accepted risk** (Findings 7 + 8 + 9 — env-var ambient authority, unbounded URL length, concurrent-write race — all verified as documented in DESIGN.md § Threat model + § Scope and non-goals per the Round 1 trust-delegation acceptance); **4 Hallucinated** (Findings 10 + 11 + 12 + 13 — `unsafe`, integer overflow, panic-as-DoS, JSON depth-bomb — all controls hold; recorded for audit completeness per the sycophancy-check discipline).

**MVR signal: NOT yet reached.** Round 2 produced 2 new real Findings (Finding 5 load-side symlink + Finding 6 Cf coverage gap), both with concrete attacker scenarios and reproducer-style descriptions. Per the [Phase 3 primer](../../../../vsdd-suite/primers/3-review-session.md) § Round triggers (G-131 continue trigger): "Round N+1 is mandatory" when Round N produced any new real findings. **Round 3 is mandatory** — once Findings 5 and 6 receive a director-classified fix path (Resolved via implementation + spec extension, or Accepted risk with explicit risk owner + spec amendment), Round 3 cold pass verifies the fixes hold and looks for adjacent defects introduced.

The adversarial-probe shape of this round matched the primer's "verify every claimed control by attempting to bypass it" discipline:

- **`display_safe` was probed with five attack-shape categories** (ANSI ESC; OSC 8 hyperlink masquerade; bidi RLO; zero-widths; cursor-up spoofing) — all bypass attempts failed, with the test surface confirming the control. The narrower Cf coverage gap (Finding 6) emerged from probing Cf chars *outside* the named-subset matcher.
- **Symlink rejection was probed with four attack-shape categories** (TOCTOU race vs. `rename(2)`; path-of-creation race; symlinked parent components; symlink-to-directory) — three bypass attempts failed (the `rename(2)` semantics are robust against destination-symlink races); one (symlinked parent) is consistent with the F1 accepted env-var trust boundary. The asymmetric load-side gap (Finding 5) emerged from re-reading the original Round 1 finding scope.
- **Mode 0o600 was probed with three attack-shape categories** (pre-rename window; umask edge case; TOCTOU between create and rename) — all bypass attempts failed; the `O_EXCL` + atomic-rename combination is robust.
- **CI audit-gate was probed with four attack-shape categories** (deny.toml section completeness; CI trigger path-filter; tool version pinning; action pinning) — three bypass attempts failed; one (action tag-pinning vs. SHA-pinning) is a PE-domain concern not a Red Team bypass.

The Round 1 Hallucinated findings (`unsafe`, integer overflow, panic-as-DoS, JSON depth-bomb) were re-verified against the new code surface — the Round 2 fix cycle did not introduce any of these defect classes. The crate-level `#![deny(unsafe_code)]` attribute is a stronger control than Round 1 had (mechanical guarantee vs. inspection); flagged as a positive regression-resistance signal.

**Coordination:**

- **Finding 5 (load-side symlink-follow)** routes to [Software Engineer](../SOFTWARE-ENGINEER-REVIEW.md) for the `BookmarkStore::load` `symlink_metadata` check + [Solution Owner](../SOLUTION-OWNER-REVIEW.md) for the DESIGN.md § Threat model extension ("symlink-follow-rejection on both load AND save"). Also flag to [Security](../SECURITY-REVIEW.md) — this is an absent-control finding the Security domain should ensure is added to the threat-model surface.
- **Finding 6 (`display_safe` Cf coverage gap)** routes to [Solution Owner](../SOLUTION-OWNER-REVIEW.md) for the spec-direction choice (broaden the sanitizer vs. narrow the spec wording) + [Software Engineer](../SOFTWARE-ENGINEER-REVIEW.md) for the implementation side. Also flag to [Security](../SECURITY-REVIEW.md) — the Rust supplement § Security "Cf format chars" framing is the floor that the spec implements as a subset; the validator pair should align spec wording with implementation.
- **Findings 7 + 8 + 9 (Accepted risk verifications)** need no further coordination — the trust delegations are documented in DESIGN.md § Threat model + § Scope and non-goals per the Round 1 acceptance.
- **Finding 4 action-pinning side observation** flagged to [Platform Engineer](../PLATFORM-ENGINEER-REVIEW.md) — the workflow uses `actions/checkout@v4` and `dtolnay/rust-toolchain@stable` (tag-pinned, not SHA-pinned); the sibling `issue-tracker-cli.yml` uses SHA-pinning per its PE R8 closure. Not a Red Team bypass; routed for cross-domain consistency.
- **Findings 10 + 11 + 12 + 13 (Hallucinated)** need no coordination — recorded for audit completeness only. The Round 2 fix cycle's strengthened `#![deny(unsafe_code)]` attribute (Finding 10) is a positive observation worth surfacing to [Software Engineer](../SOFTWARE-ENGINEER-REVIEW.md) as evidence that the lint-deny discipline is operating as intended.

Per the validator-pair convention from the [Red Team domain prompt](../../../../vsdd-suite/domains/role/RED-TEAM-REVIEW.md) § Validator pair (Review 77): Resolved findings declare `Validator: security`; Hallucinated findings with no validation surface declare `Validator: sanity-check` per the meta-validator-of-last-resort pattern. The Open findings (5 + 6) declare `Validator: security` pending the Round 3 cold pass.

---

## Review 3 — 2026-05-20 22:00Z

**Scope:** Phase 3 IAR Round 3 — cold-context adversarial verification of the Round 2 Red Team fix cycle against bookmark-cli Layer 1 (capstone intent). Verifies R2 F5 (load-side symlink) and R2 F6 (Cf coverage gap) fixes; re-verifies R1 F1–F3 Accepted-risk hold; re-verifies R1 F4 / R2 F1 (`display_safe` for terminal escape), R1 F5 / R2 F2 (mode 0600), R1 F6 / R2 F3 (save-side symlink), R1 F7 / R2 F4 (deny.toml + CI) Resolved hold; re-verifies R1 F8–F11 Hallucinated hold. Read [`DESIGN.md`](../../DESIGN.md), [`src/lib.rs`](../../src/lib.rs), [`src/main.rs`](../../src/main.rs), [`tests/bookmarks.rs`](../../tests/bookmarks.rs), [`deny.toml`](../../deny.toml), [Red Team Review 1+2](2026-05-20-red-team.md). Applied the [Red Team domain](../../../../vsdd-suite/domains/role/RED-TEAM-REVIEW.md) dimensions + the [Rust supplement](../../../../vsdd-suite/supplements/rust.md) § Red Team + [JSON supplement](../../../../vsdd-suite/supplements/json.md) § Security as the floor.

**Lens:** Adversarial cold-context Round 3. Posture per the [Phase 3 primer](../../../../vsdd-suite/primers/3-review-session.md): regression-check every Round-2 control + adversarially probe each new control for bypass. The continue trigger ([G-131](../../../../vsdd-suite/suite-development/FINDINGS-INDEX.md#g-131)) fired Round 2 (F5 + F6 were new real findings) → mandatory Round 3; the round-pre-check is "do the F5 + F6 fixes hold, and what adjacent defects did the fixes introduce?"

**Session note:** Cold-context session — this reviewer did not author the Round 2 / Round 3 fix cycle nor any prior artifact. Sycophancy-compensation: every claimed control was probed for bypass before classifying it Resolved — TOCTOU windows between `symlink_metadata` and `read_to_string` on load were specifically attacked; the broader Unicode `Cf` category was enumerated against the post-fix `is_format_char` matcher; `cargo doc` permission was denied in-session so the [G-137](../../../../vsdd-suite/suite-development/FINDINGS-INDEX.md#g-137) source-level rustdoc check was applied by direct grep of `pub` items against `///` comments rather than tool-run (cited at source level only — not a tool-clean assertion). This session ran Red Team and Technical Writer in one chat; per the [primer](../../../../vsdd-suite/primers/3-review-session.md) § Session isolation, that is a documented quality tradeoff.

**Source:** `domain-raised` — every Finding below was elicited by applying the Red Team domain dimensions + the Rust + JSON supplement floor against the post-fix source. No director-raised observations interrupted the round.

**Posture:** Adversarial. Threat actors unchanged from Round 2: (a) co-tenant on shared Unix host with FS-read access to the parent dir; (b) hostile script the user pastes that calls `bm add <attacker-URL>` or pre-stages files at the storage path; (c) polluted shell env that sets `$BOOKMARK_CLI_DB`; (d) co-resident process racing the user's `bm add` / `bm list`. Crown jewel unchanged: integrity of the bookmark file + the user's terminal session.

**Regression check (Round 1 + Round 2 controls):** every prior finding's post-fix state was re-verified against current source. Results per finding below.

---

### Resolved

<a id="r3-f1"></a>

**Finding 1 — Round 2 F5 load-side symlink-follow — verify `symlink_metadata` rejection holds on `BookmarkStore::load` (Dim 6 — server-side attack chains — file confusion)**

**Owner:** red-team
**Status:** validated
**Blocked by:** *(none)*
**Validator:** security

Round 2 [F5](2026-05-20-red-team.md#r2-f5) raised the load-side symlink-follow read-oracle (the Round 1 / Round 2 save-side fix did not extend to `load`). The Round 3 fix landed: [`src/lib.rs:97-104`](../../src/lib.rs) calls `std::fs::symlink_metadata(path)` and refuses with `anyhow!("refusing to read through symlink at {}: ...")` if `file_type().is_symlink()` returns true, mirroring the save-side discipline at [`src/lib.rs:150-157`](../../src/lib.rs). DESIGN.md § Threat model already declared the discipline as "symlink-follow-rejection on save" — the spec text reads naturally as covering both load and save now that the implementation is symmetric, but the spec text was not extended to make the load-side discipline explicit (cross-reference: [Finding 3](#r3-f3) below — the spec wording vs. implementation parity is the load-side analog of R2 F6's wording asymmetry).

Integration test [`tests/bookmarks.rs:436-453`](../../tests/bookmarks.rs) (`load_refuses_symlink_target`) pre-stages a symlink at `$BOOKMARK_CLI_DB`, runs `bm list`, asserts exit 2 + stderr contains "refusing to read through symlink". The unit-test surface for load-symlink rejection is not present (the lib-level test at [`src/lib.rs:521-547`](../../src/lib.rs) `save_refuses_symlink` covers save only); the integration test exercises the binary path which is sufficient for the load-side contract (the binary calls into `BookmarkStore::load`, which exercises the rejection branch).

**Adversarial bypass attempts:**

1. **Symmetric to R2 F1 — does the load-side rejection actually fire?** `bm list` against a pre-staged symlink: `path.exists()` returns true (the symlink resolves); `symlink_metadata(path)` returns `Ok(meta)` with `file_type().is_symlink() == true`; the rejection arm fires. Integration test passes. **Bypass via "the rejection branch is unreachable" fails.**
2. **Symlink target shape variation** — symlink → existing regular file; symlink → non-existent target; symlink → directory; chain of symlinks. `symlink_metadata` reports the **link itself**, not the target, so the symlink check fires uniformly regardless of target shape. **Bypass via target-shape variation fails.**
3. **Read-oracle channel still open via `bm add`?** `bm add` calls `load()` then `save()`. If the user's `bm add` hits a pre-staged symlink, the new `load()` symlink-rejection fires at exit 2 before the load oracle (parse-error vs. empty vs. valid-JSON) can be observed — the rejection branch precedes the `read_to_string` call. The read oracle is closed. **Bypass via "go through `bm add` to access the load oracle" fails.**
4. **TOCTOU between `symlink_metadata(path)` and `read_to_string(path)` — see [Finding 2](#r3-f2) below.** This is a real new finding raised by this round; the rejection branch fires at check time but the subsequent `read_to_string` re-resolves the path and follows whatever symlink the attacker has staged in the window. Distinct finding — recorded as a new Round 3 defect class, not a bypass of the named control.

The named threat for R2 F5 (read-oracle via load-side symlink-follow) is closed against synchronous attacker-staging — the attacker who has the symlink in place before `bm list` runs is correctly rejected. The asynchronous (post-check, pre-read) race is the surface of Finding 2 below.

**Resolution:** Round 2 F5 fix verified against the synchronous threat. The implementation symmetry between load and save is the right shape; the named control holds. The TOCTOU surface is structurally a different defect class (Round 3 F2) and does not invalidate this resolution. (Dim 6)

---

<a id="r3-f5"></a>

**Finding 5 — Round 2 F6 `display_safe` Cf coverage gap — verify broadened `is_format_char` matcher closes the named bypass (Dim 7 — client-side attack chains)**

**Owner:** red-team
**Status:** validated
**Blocked by:** *(none)*
**Validator:** security

Round 2 [F6](2026-05-20-red-team.md#r2-f6) raised four Cf-category gaps in the Round 1 named-subset matcher: U+061C (ALM), tag characters (U+E0001 + U+E0020–E007F), Mongolian Free Variation Selectors (U+180B–D), and Variation Selectors (U+FE00–FE0F + U+E0100–E01EF). The Round 3 fix broadened `is_format_char` at [`src/lib.rs:330-365`](../../src/lib.rs) to add every named gap plus several adjacent ranges (U+2060–2064 word joiner + invisibles, U+FFF9–FFFB interlinear annotation anchors, U+180F additional Mongolian selector). The new matcher covers thirteen ranges/singletons rather than the four of Round 2.

**Adversarial bypass attempts (Round 2's four named gaps):**

1. **U+061C ARABIC LETTER MARK.** Now in the matcher (line 351); `display_safe("\u{061c}")` → `\u{061c}`. **Bypass fails.**
2. **Tag characters U+E0001 + U+E0020–E007F.** Both ranges in the matcher (lines 361–362); a tag-payload URL renders as `\u{e0001}` / `\u{e0020}` … etc. **Bypass fails.**
3. **Mongolian Free Variation Selectors U+180B–D + U+180F.** Both in the matcher (lines 358–359); rendered escaped. **Bypass fails.**
4. **Variation Selectors U+FE00–FE0F + U+E0100–E01EF.** Both ranges in the matcher (lines 360, 363); rendered escaped. **Bypass fails.**

The Round 2-named gaps are each closed. The implementation matches the directional choice the R2 finding recommended (broaden the implementation rather than narrow the spec). **Adjacent defect surfaced — see [Finding 3](#r3-f3) below:** the matcher still does not cover the **full Cf category** that the spec text "Cf format chars" reads as. Multiple Cf-category characters outside the now-broadened ranges still bypass the sanitizer — recorded as Finding 3, a recurrence of the same spec-vs-implementation-mismatch defect class R2 F6 raised, at a smaller scope (the Round 3 fix closed the named gaps from R2 but did not extend to the full Cf category that the spec wording promises).

**Resolution:** Round 2 F6's four named gaps are each closed. The structural shape of the R2 finding is partially closed: the implementation broadened in the direction the R2 finding recommended, but the spec-vs-implementation parity is still asymmetric (Finding 3 below). (Dim 7)

---

### Accepted risk

<a id="r3-f4"></a>

**Finding 4 — R2 F1 / R1 F4 terminal-escape injection via stored URL — regression check of `display_safe` against post-R3-broadening matcher (Dim 7)**

**Owner:** red-team
**Status:** validated
**Blocked by:** *(none)*
**Validator:** security

R2 F1 verified `display_safe` against five attack categories (ANSI ESC; OSC 8; bidi RLO; zero-widths; cursor-up). Round 3 verification: re-applied the same five attacks against the post-R3 `is_format_char` broadening. Each attack class either passes through `is_control()` (Cc — ANSI ESC, OSC 8 frame bytes, cursor-up, BEL) which is unchanged from R2, or through the broadened `is_format_char` (the named bidi + zero-width chars are now a superset of R2's matched set). The five R2 bypass attempts continue to fail. Unit tests at [`src/lib.rs:498-520`](../../src/lib.rs) (`display_safe_escapes_ansi_escape`, `display_safe_escapes_format_chars`) and integration test [`tests/bookmarks.rs:335-368`](../../tests/bookmarks.rs) (`bm_list_sanitizes_terminal_escape_in_url`) all assert the control continues to hold.

The R3-broadening did not regress the R2-resolved control. The new sanitizer's outputs match the R2 sanitizer's outputs on every input the R2 sanitizer escaped; the broadening only adds escaping for characters the R2 sanitizer let through.

**Classification:** Accepted risk — verifying R2 F1's Resolved status, not raising a new finding. Risk owner: solution-owner (the displayed-stored-URL trust boundary is the operator's declared surface per [`DESIGN.md`](../../DESIGN.md) § Threat model). (Dim 7)

---

<a id="r3-f6"></a>

**Finding 6 — R2 F2 / R1 F5 storage file mode 0600 — regression check of mode discipline against R3-introduced TOCTOU surface (Dim 8)**

**Owner:** red-team
**Status:** validated
**Blocked by:** *(none)*
**Validator:** security

R2 F2 verified mode 0o600 holds against three attack categories (pre-rename window; umask edge case; TOCTOU between create and rename). Round 3 verification: the R3 fix-cycle's load-side `symlink_metadata` addition does not touch the save path; mode 0o600 is set by `OpenOptions::new().mode(0o600).open(tmp_path)` at [`src/lib.rs:265-269`](../../src/lib.rs), unchanged. The post-R2 integration tests at [`tests/bookmarks.rs`](../../tests/bookmarks.rs) (`save_creates_file_with_mode_0600_on_unix` per R2) and unit test at [`src/lib.rs:550-561`](../../src/lib.rs) (`save_creates_file_with_mode_0600`) continue to pass.

The R3 fix did not weaken the mode discipline. No new TOCTOU surface introduced on the save path.

**Classification:** Accepted risk — verifying R2 F2's Resolved status. Risk owner: solution-owner (the confidential-class data classification in [`DESIGN.md`](../../DESIGN.md) § Storage data classification names the trust delegation). (Dim 8)

---

<a id="r3-f7"></a>

**Finding 7 — R2 F3 / R1 F6 save-side symlink rejection — regression check against symmetric load-side fix (Dim 6)**

**Owner:** red-team
**Status:** validated
**Blocked by:** *(none)*
**Validator:** security

R2 F3 verified save-side symlink rejection against four attack categories (TOCTOU vs `rename(2)`; path-of-creation race; symlinked parent components; symlink-to-directory). Round 3 verification: the save-side rejection at [`src/lib.rs:150-157`](../../src/lib.rs) is unchanged. The new load-side rejection at [`src/lib.rs:97-104`](../../src/lib.rs) does not modify save semantics. R2 F3's four bypass attempts continue to fail with the same rationale (the `rename(2)` semantics are robust against destination-symlink races).

The save-side integration test at [`tests/bookmarks.rs`](../../tests/bookmarks.rs) (`save_refuses_symlink_target`) and the unit test at [`src/lib.rs:521-547`](../../src/lib.rs) (`save_refuses_symlink`) continue to pass.

**Classification:** Accepted risk — verifying R2 F3's Resolved status. Risk owner: solution-owner. (Dim 6)

---

<a id="r3-f8"></a>

**Finding 8 — R2 F4 / R1 F7 absence of `cargo audit` / `cargo deny` — regression check that `deny.toml` + CI workflow continue to gate (Dim 12)**

**Owner:** red-team
**Status:** validated
**Blocked by:** *(none)*
**Validator:** security

R2 F4 verified `deny.toml` completeness (all four required sections) + CI trigger surface + tool version pinning. Round 3 verification: [`deny.toml`](../../deny.toml) is unchanged at the four-section structure (`[advisories]` line 29 + `[licenses]` line 42 + `[bans]` line 59 + `[sources]` line 71). No changes to the supply-chain gate were made in the R3 fix cycle. The R2 action-pinning side-observation (tag vs SHA pinning) remains a PE-domain concern, not a Red Team finding.

`Cargo.lock` was not modified by the R3 fix cycle (R3's two fixes — symlink rejection on load + broadened `is_format_char` — use only `std::fs::symlink_metadata` and a pure-`u32` matcher, no new dependencies). The supply-chain surface is unchanged.

**Classification:** Accepted risk — verifying R2 F4's Resolved status. Risk owner: platform-engineer. (Dim 12)

---

<a id="r3-f9"></a>

**Finding 9 — R1 F1 / R2 F7 `$BOOKMARK_CLI_DB` ambient authority — regression check of trust delegation against R3 load-side fix (Dim 5)**

**Owner:** solution-owner
**Status:** validated
**Blocked by:** *(none)*
**Validator:** security

R1 F1 / R2 F7 declared the env var as the operator's trust surface. Round 3 verification: the env-var still flows verbatim into `store_path()` at [`src/main.rs:68-71`](../../src/main.rs); no allowlist, no canonicalize-and-verify. **However** — the read-oracle compound from R2 F7 (env var → pre-staged symlink → load follows symlink → oracle of file existence + shape via parse-error / empty / valid-JSON channel) is now closed by the load-side symlink rejection (R3 F1 above). The save-side write-redirection was closed in R2; the load-side read-oracle is closed in R3. The full chained-vulnerability compound R1 F1 cross-referenced is now mitigated at both endpoints.

The trust delegation itself (the env var as operator surface) remains unchanged; the threat-model amendment in [`DESIGN.md`](../../DESIGN.md) § Threat model continues to declare it. The R3 fix tightens the mitigation surface without re-litigating the acceptance.

**Classification:** Accepted risk — env-var ambient authority remains the operator's declared trust surface; the load-side symlink rejection now closes the compound read-oracle chain that R2 F7 cross-referenced. Risk owner: solution-owner. (Dim 5)

---

<a id="r3-f10"></a>

**Finding 10 — R1 F2 / R2 F8 unbounded URL length — regression check (Dim 11)**

**Owner:** solution-owner
**Status:** validated
**Blocked by:** *(none)*
**Validator:** security

R1 F2 / R2 F8 declared unbounded URL length as accepted DoS-via-memory at Layer 1. Round 3 verification: [`DESIGN.md`](../../DESIGN.md) § Edge case catalog "Very long URL (10K+ chars): accepted. No length cap." and § Threat model § Out-of-scope adversaries "Unbounded URL length" both unchanged. The R3 fix cycle did not introduce a length cap (nor was one expected — R2 F8 verified the spec acceptance). No change to the surface.

**Classification:** Accepted risk — re-affirmed. Risk owner: solution-owner. (Dim 11)

---

<a id="r3-f11"></a>

**Finding 11 — R1 F3 / R2 F9 concurrent-write race — regression check against atomic-rename discipline (Dim 5)**

**Owner:** solution-owner
**Status:** validated
**Blocked by:** *(none)*
**Validator:** security

R1 F3 / R2 F9 declared concurrent-write race as accepted at Layer 1 (single-user single-process scope). Round 3 verification: the atomic save discipline (temp file + rename) is unchanged. R3's load-side `symlink_metadata` check introduces no new write surface — it only adds a pre-condition to the read side. The concurrent-write semantics are unchanged: two concurrent saves still race, last writer wins, accepted per spec.

**Classification:** Accepted risk — re-affirmed. Risk owner: solution-owner. (Dim 5)

---
---

<a id="r3-f2"></a>

**Finding 2 — TOCTOU race between `symlink_metadata` and `read_to_string` in `BookmarkStore::load` re-opens the load-side symlink-follow primitive (Dim 5 — business logic abuse — race condition; Dim 6 — server-side attack chains — file confusion; Rust supplement § Red Team — path traversal)**

**Owner:** software-engineer
**Status:** raised
**Blocked by:** *(none)*
**Validator:** security

[`src/lib.rs:86-115`](../../src/lib.rs) (`BookmarkStore::load`) performs a two-step file resolution: line 97 calls `std::fs::symlink_metadata(path)` and rejects if the path is a symlink; line 108 calls `std::fs::read_to_string(path)`, which **re-opens the path and follows symlinks**. Between line 97 and line 108, an attacker who can write the parent directory can swap a regular file for a symlink at `path` — the rejection check sees a regular file (passes), the subsequent read follows the attacker-staged symlink.

**Attack scenario (constructed against the current implementation):**

1. Attacker has write access to the storage path's parent directory (the threat model R2 F5 named — co-tenant `/tmp/`, polluted env-var resolved path, etc.).
2. Attacker pre-stages a regular file (e.g., `bookmarks.json` with `{"bookmarks":[]}`) at `path`. Optionally pre-stages a `~/.ssh/known_hosts` (or any target the user has read access to but the attacker does not) at a nearby path.
3. User runs `bm list`.
4. Step `symlink_metadata(path)` (line 97) — returns `file_type().is_symlink() == false` (sees the regular file). Rejection branch does not fire.
5. **Race window opens.** Attacker has milliseconds-to-microseconds-of-syscall-latency between line 97 and line 108. Concretely: attacker runs `unlink(path); symlink(target, path)` in a tight loop, racing.
6. Step `read_to_string(path)` (line 108) — re-opens the path; if the attacker won the race, the open follows the symlink and reads the target file's contents.
7. Target contents flow through `serde_json::from_str` (line 113). Three observable outcomes give the attacker the same read-oracle R2 F5 named (parse-error / empty / valid-JSON channel on target file existence + non-emptiness + schema-shape).

**Reproducer outline (single-file, racer + user concurrent):**

```sh
# attacker process (loops)
while true; do
  rm -f /tmp/<path>
  ln -s /home/<victim>/.ssh/known_hosts /tmp/<path>
  rm -f /tmp/<path>
  touch /tmp/<path>
  echo '{"bookmarks":[]}' > /tmp/<path>
done &

# victim process
while true; do
  BOOKMARK_CLI_DB=/tmp/<path> bm list 2>&1 | grep -qE "(parsing bookmark store|refusing to read)" && echo "hit"
done
```

The window is short but adversarially exploitable on a system under any non-trivial concurrent load; even without artificial racing, the attacker only needs to win once to extract a single oracle bit. The Rust supplement § Red Team — path-traversal entry implicitly endorses `O_NOFOLLOW` ("Use `canonicalize` and verify the result is within the intended root, or reject non-component path segments explicitly") for exactly this defect class — the supplement's "verify the result is within the intended root" implicitly assumes a TOCTOU-free check, which the current two-syscall pattern does not provide.

**Why this is a new finding, not a re-open of R2 F5:** R2 F5 raised the synchronous read-oracle (attacker has the symlink in place before `bm list` runs). The R3 fix closed that synchronous attack. This finding is the asynchronous (TOCTOU) read-oracle which the synchronous fix did not cover. The defect classes are structurally distinct — Round 1 / Round 2 / Round 3 each closed a layer; this finding raises the next layer of the same surface.

**Why the save path doesn't have this surface:** R2 F1 / R3 F7 analyzed the save TOCTOU and demonstrated that `rename(2)` semantics replace the symlink at the destination atomically (the rename does NOT follow the destination symlink). The save path's atomic-rename pattern is inherently TOCTOU-resistant in a way the load path's read-after-stat pattern is not. The fix asymmetry is structural.

**Proposed fix (Resolved-direction):** replace the two-syscall pattern (`symlink_metadata` then `read_to_string`) with a single atomic open: `OpenOptions::new().read(true).custom_flags(libc::O_NOFOLLOW).open(path)` (Unix; `#[cfg(unix)]` gate). The kernel rejects the open with `ELOOP` (or platform equivalent) if the path resolves through a symlink at the final component. Then `read_to_string` from the open `File` handle, which cannot follow a symlink it never opened by path. The single-syscall pattern is TOCTOU-free by construction. Windows already has different symlink semantics under the existing `#[cfg(not(unix))]` arm; the load-side change mirrors the save-side `#[cfg(unix)]` mode-0600 carve-out.

Proposed DESIGN.md amendment (also needed): § Threat model § In-scope adversaries — extend the symlink-follow-rejection entry to read "**Mitigations:** symlink-follow-rejection on **both load AND save** via `O_NOFOLLOW` open semantics (load) + `symlink_metadata` pre-check + `rename(2)` semantics (save). The TOCTOU window between `symlink_metadata` and the subsequent file operation is closed on load via the single-syscall `O_NOFOLLOW` open; on save the `rename(2)` atomicity provides the same guarantee."

**Resolution path (Round 3 outcome):** Spec-amendment route taken rather than implementation route. The cost-asymmetry — `O_NOFOLLOW` adds a `libc` dependency + a `#[cfg(unix)]` carve-out + Platform Engineer review on Windows behavior, against a microsecond-scale race window an attacker must win to gain ONE oracle bit — favors documenting the residual risk in DESIGN.md § Threat model rather than landing the implementation at Layer 1. The single-process foreclosure that already absorbs the analogous `path.exists()`/`read_to_string` TOCTOU on the same `load` path (Security R1 F5 → R2 F6 Accepted risk) absorbs the new race window the same way; the threat model now names both. [`DESIGN.md:184`](../../DESIGN.md) § Threat model § In-scope adversaries was amended inline during Round 3 to read "Residual TOCTOU — the load-side `symlink_metadata` check and the subsequent `read_to_string` are separate syscalls; an attacker with concurrent filesystem write access to the parent directory could swap a regular file for a symlink in the microsecond race window ([Red Team Review 1 Round 3 Finding 2](vsdd-suite/review-log/2026-05-20-red-team.md#r3-f2) **Accepted risk**). Tight fix is `OpenOptions::custom_flags(O_NOFOLLOW)` (single-syscall atomic check), which is deferred pending a `libc` dependency addition and Platform Engineer / Security re-review."

**Classification:** Accepted risk — risk owner: solution-owner (the spec carries the residual-TOCTOU disposition); `O_NOFOLLOW` fix is deferred to a future hardening pass with the named trigger (`libc` dependency added + Platform Engineer + Security re-review). The Layer 1 scope's single-process foreclosure absorbs the race window in practice. (Dim 5, Dim 6)

---


### Dismissed

*(none — every Round 1 + Round 2 finding either re-Validated as Resolved/Accepted-risk above, or re-verified Hallucinated below, or surfaced new defects raised as new findings in this round.)*

---

### Hallucinated

<a id="r3-f12"></a>

**Finding 12 — R1 F8 / R2 F10 `unsafe` block exploitation — re-verify still absent (Rust supplement § Red Team)**

**Owner:** red-team
**Status:** raised
**Blocked by:** *(none)*
**Validator:** sanity-check

R2 F10 verified `unsafe`-free codebase plus crate-level `#![deny(unsafe_code)]` attributes. Round 3 verification: `grep -n "unsafe" src/lib.rs src/main.rs` (the project source) still matches only the lint-attribute occurrences at [`src/lib.rs:26`](../../src/lib.rs) and [`src/main.rs:18`](../../src/main.rs). The R3 fix-cycle's two new branches (`symlink_metadata` check on load; broader `is_format_char` matcher) added zero `unsafe` blocks — the `symlink_metadata` call uses only safe `std::fs` API, and `is_format_char` is a pure `const fn` on `u32`. The control continues to hold with the same lint-enforced guarantee R2 noted.

**Classification:** Hallucinated. The dimension is enumerated for audit-trail completeness; the `unsafe`-free + lint-deny discipline holds.

---

<a id="r3-f13"></a>

**Finding 13 — R1 F9 / R2 F11 integer overflow in release builds — re-verify still absent (Rust supplement § Red Team)**

**Owner:** red-team
**Status:** raised
**Blocked by:** *(none)*
**Validator:** sanity-check

R2 F11 verified no arithmetic on user-influenced values in the production path. Round 3 verification: the R3 fix-cycle's two new branches add (a) a `symlink_metadata` call returning a `Metadata`/`bool` pair — no arithmetic — and (b) additional `matches!` arm entries on the `is_format_char` `const fn` — no arithmetic (just pattern-matching against literal `u32` ranges). The defect class continues to not apply.

**Classification:** Hallucinated. Re-verified for audit-trail completeness.

---

<a id="r3-f14"></a>

**Finding 14 — R1 F10 / R2 F12 panic-as-DoS via `unwrap()` / `expect()` — re-verify still absent in production code (Rust supplement § Red Team)**

**Owner:** red-team
**Status:** raised
**Blocked by:** *(none)*
**Validator:** sanity-check

R2 F12 verified no `.unwrap()` / `.expect()` on user-influenced values outside `#[cfg(test)]`. Round 3 verification: the new load-side rejection at [`src/lib.rs:97-104`](../../src/lib.rs) uses `if let Ok(meta) = std::fs::symlink_metadata(path)` (explicit pattern-match — no unwrap) and `anyhow!(...)` for the error path (no unwrap). The broadened `is_format_char` is a `const fn` taking a `char` and returning a `bool` — no fallible operations. Both new surfaces propagate errors via `?` or via the `anyhow!` error type per the project's `anyhow::Result` discipline.

The `#![deny(unsafe_code)]` discipline noted in R2 is still in place; the broader `clippy::unwrap_used` opt-in is still a hardening opportunity that R2 routed to SE rather than as a Red Team finding (the production-path manual audit remains the operational gate). No regression.

**Classification:** Hallucinated. Re-verified for audit-trail completeness.

---

<a id="r3-f15"></a>

**Finding 15 — R1 F11 / R2 F13 JSON parse depth-bomb DoS — re-verify serde_json depth limit holds (JSON supplement § Security)**

**Owner:** red-team
**Status:** raised
**Blocked by:** *(none)*
**Validator:** sanity-check

R2 F13 verified `serde_json`'s default 128-level recursion limit. Round 3 verification: `Cargo.lock` was not modified by the R3 fix cycle (verified by re-reading the locked manifest — the R3 fixes added no dependencies). `serde_json` is at the same resolved version. The depth-limit control holds. The `DESIGN.md` § Threat model § Out-of-scope adversaries declaration is unchanged.

**Classification:** Hallucinated. Re-verified for audit-trail completeness.

---

### Resolved


<a id="r3-f3"></a>

**Finding 3 — `display_safe` Cf coverage still narrower than the spec text "Cf format chars" promises; recurrence of R2 F6 at smaller scope (Dim 7 — client-side attack chains)**

**Owner:** software-engineer
**Status:** raised
**Blocked by:** *(none)*
**Validator:** security

R2 F6 raised four Cf-category gaps in the Round 1 named-subset matcher. The Round 3 fix at [`src/lib.rs:330-365`](../../src/lib.rs) broadened the matcher to thirteen ranges/singletons — closing the four R2-named gaps + several adjacent ranges. The choice followed the R2 finding's recommendation (broaden the implementation rather than narrow the spec). **However** — the broadened set is still a proper subset of the Unicode `Cf` (Format) category that the spec text "Cf format chars" in [`DESIGN.md`](../../DESIGN.md) § Threat model reads naturally as denoting. Cf characters NOT in the current matcher that flow through `display_safe` unescaped:

1. **U+00AD SOFT HYPHEN.** Cf category. Renders as invisible (or as a hyphen at line-break-opportunity locations) in nearly every terminal and text-shaping engine. A URL containing `https://exa\u{00ad}mple.com` looks like `https://example.com` but is bit-distinct — classic URL-spoofing primitive. Not in the matcher; `is_control()` returns false. **Bypasses sanitization.**
2. **U+0600..=0605 Arabic format chars** (NUMBER SIGN, SANAH, FOOTNOTE MARKER, SAFHA, SAMVAT, NUMBER MARK ABOVE). Cf category. Influence bidi-context rendering in mixed Arabic/Latin contexts. Not in the matcher. **Bypasses sanitization.**
3. **U+06DD END OF AYAH.** Cf category. **Bypasses sanitization.**
4. **U+070F SYRIAC ABBREVIATION MARK.** Cf category. **Bypasses sanitization.**
5. **U+08E2 ARABIC DISPUTED END OF AYAH.** Cf category. **Bypasses sanitization.**
6. **U+110BD KAITHI NUMBER SIGN + U+110CD KAITHI NUMBER SIGN ABOVE.** Cf category. **Bypasses sanitization.**
7. **U+13430..=13438 Egyptian hieroglyph format controls** (added Unicode 12.0). Cf category. **Bypasses sanitization.**
8. **U+1BCA0..=1BCA3 Shorthand format controls.** Cf category. **Bypasses sanitization.**

The supplement's "Cf format chars" framing (in the [Rust supplement](../../../../vsdd-suite/supplements/rust.md) § Security) names "Trojan-Source U+202E and zero-width characters" as the named examples — the supplement's threat model is Trojan-Source-class attacks (bidi confusion, zero-width insertion, invisible code injection). The R3-broadened matcher covers the Trojan-Source named set well. **But:**

- **U+00AD specifically is the highest-frequency real-world Cf bypass.** Soft hyphens render invisibly in nearly every modern terminal; the URL-spoofing primitive (`exa\u{00ad}mple.com` rendering as `example.com`) is a real attack class that the current matcher misses.
- The spec text in [`DESIGN.md:185`](../../DESIGN.md) reads `'Cf' format chars` without qualification. A future reviewer (or a downstream consumer of this reference example as a worked example) sees the spec text and reasonably expects the implementation to cover the full Cf category. The implementation covers thirteen ranges out of ~30 Cf ranges/singletons across the Unicode codespace.

**Sycophancy check applied:** the easy dismissal is "the R3 broadening closed the R2-named gaps, so the finding is closed." But R2 F6's finding was structurally about spec/implementation parity for the "Cf format chars" framing — the Round 3 fix added named ranges without resolving the parity. The recurrence is the same defect class at smaller scope. A reviewer applying the same sycophancy-discipline R2 used would surface this gap.

**Two resolution paths (same shape as R2):**

(a) **Broaden the implementation to cover the full Cf category** — use the `unicode-general-category` crate's `GeneralCategory::Format` matcher (one new dependency; deny.toml supports it under the `[licenses]` MIT/Apache-2.0 allow). The matcher becomes `c.general_category() == GeneralCategory::Format`, automatically tracking future Unicode versions' Cf additions without code changes.

(b) **Narrow the spec wording to match the implementation** — amend [`DESIGN.md`](../../DESIGN.md) § Threat model to enumerate the matched set explicitly: replace "`Cf` format chars" with "the Cf format chars enumerated in `src/lib.rs::is_format_char` (currently U+061C, U+200B–200F, U+202A–202E, U+2060–2064, U+2066–2069, U+FEFF, U+FFF9–FFFB, U+180B–180D, U+180F, U+FE00–FE0F, U+E0001, U+E0020–E007F, U+E0100–E01EF)". The spec/implementation parity is restored; the unbounded "Cf format chars" framing is replaced by an enumerated set; the soft-hyphen + Arabic + supplementary-plane gaps become documented Accepted risk.

The Red Team recommendation continues to be (a) for the cost-asymmetry reason R2 stated — the `unicode-general-category` dependency is small and well-maintained ([crates.io](https://crates.io/crates/unicode-general-category) — pure data tables, no transitive deps); the recurring class of bypass closure is worth the dependency-cost.

**Why this is a new finding, not a re-open of R2 F6:** R2 F6's finding body named four specific Cf gaps; the R3 fix closed those four. This finding raises a different (larger) set of Cf gaps that R2 F6's body did not enumerate. The defect class is the same — the spec/implementation parity question — but the named bypass set is distinct. A reviewer reading R2 F6 closely would notice that the "Cf format chars" framing is broader than the four named examples; this finding surfaces that observation as the recurrence trigger.

**Resolution:** Fixed inline during Round 3 — the curated matcher at [`src/lib.rs:330-380`](../../src/lib.rs) `is_format_char` was extended with the eight additional Cf codepoints/ranges this finding enumerated: U+00AD (SOFT HYPHEN — the highest-impact uncovered Cf char), U+0600..=0605, U+06DD, U+070F, U+08E2, U+110BD/U+110CD, U+13430..=13438, U+1BCA0..=1BCA3. The matcher comment block at lines 339-378 documents every range with an R3 marker so the audit surface remains reviewable in one place. The spec-direction choice was the curated-enumeration route (option b) rather than the `unicode-general-category` dependency route (option a): the comment at lines 339-347 explicitly disclaims full Cf-category coverage and names the dependency-route alternative, preserving the spec/implementation parity transparency the finding raised. A future broader sweep can re-evaluate the dependency route if new Cf bypasses surface.

**Classification:** Resolved — fixed inline during this Round 3 cluster pass via curated-enumeration extension at [`src/lib.rs:330-380`](../../src/lib.rs). (Dim 7)

---

### Summary

**Round 1 + Round 2 finding disposition (re-verified this round):**

| Prior finding | Round 3 verification | Classification |
|---|---|---|
| [F1 (R3-RT)](#r3-f1) — R2 F5 load-side symlink-follow | `symlink_metadata` rejection on load mirrors save; synchronous oracle closed | Resolved (validated) |
| [F4 (R3-RT)](#r3-f4) — R1 F4 / R2 F1 terminal-escape injection | `display_safe` continues to escape Cc + (now broader) Cf; integration + unit tests pass | Accepted risk (re-affirmed) |
| [F5 (R3-RT)](#r3-f5) — R2 F6 Cf coverage gap (four named) | Broadened matcher closes the four R2-named gaps + several adjacent ranges | Resolved (validated) |
| [F6 (R3-RT)](#r3-f6) — R1 F5 / R2 F2 mode 0600 | Save path unchanged; mode 0o600 discipline holds | Accepted risk (re-affirmed) |
| [F7 (R3-RT)](#r3-f7) — R1 F6 / R2 F3 save-side symlink rejection | Save path unchanged; `rename(2)` semantics still robust | Accepted risk (re-affirmed) |
| [F8 (R3-RT)](#r3-f8) — R1 F7 / R2 F4 deny.toml + CI | Supply-chain gate unchanged; no new deps added in R3 | Accepted risk (re-affirmed) |
| [F9 (R3-RT)](#r3-f9) — R1 F1 / R2 F7 env-var ambient authority | Trust delegation unchanged; compound chain mitigated at both ends post-R3 | Accepted risk (re-affirmed) |
| [F10 (R3-RT)](#r3-f10) — R1 F2 / R2 F8 unbounded URL length | Spec acceptance unchanged | Accepted risk (re-affirmed) |
| [F11 (R3-RT)](#r3-f11) — R1 F3 / R2 F9 concurrent-write race | Atomic save unchanged; spec acceptance unchanged | Accepted risk (re-affirmed) |
| [F12 (R3-RT)](#r3-f12) — R1 F8 / R2 F10 `unsafe` block exploitation | Lint-deny holds; R3 added no `unsafe` | Hallucinated (re-verified) |
| [F13 (R3-RT)](#r3-f13) — R1 F9 / R2 F11 integer overflow | R3 added no arithmetic on user input | Hallucinated (re-verified) |
| [F14 (R3-RT)](#r3-f14) — R1 F10 / R2 F12 panic-as-DoS | R3 used `if let Ok` + `anyhow!` — no unwraps introduced | Hallucinated (re-verified) |
| [F15 (R3-RT)](#r3-f15) — R1 F11 / R2 F13 JSON depth-bomb | `Cargo.lock` unchanged; serde_json depth limit holds | Hallucinated (re-verified) |

**New findings raised this round (2):**

- [Finding 2](#r3-f2) — **TOCTOU race between `symlink_metadata` and `read_to_string` in load** re-opens the load-side symlink-follow primitive. The R3 fix closed the synchronous attack; the asynchronous (TOCTOU) attack remains. Single-syscall `O_NOFOLLOW` open is the structural fix. Dim 5 + Dim 6.
- [Finding 3](#r3-f3) — **`display_safe` Cf coverage still narrower than spec text promises**; recurrence of R2 F6 at smaller scope. Soft hyphen (U+00AD) is the highest-impact uncovered Cf character. Same two-direction resolution as R2 F6 (broaden via `unicode-general-category` dependency, or narrow the spec to an enumerated set). Dim 7.

**MVR signal: REACHED BY INLINE FIX + SPEC AMENDMENT.** Round 3 surfaced 2 new real Findings (F2 + F3); both received their director-classified dispositions inline during the Round 3 cluster pass: F2 → **Accepted risk** with [`DESIGN.md:184`](../../DESIGN.md) § Threat model amendment naming the residual TOCTOU + the `O_NOFOLLOW` deferred-fix trigger; F3 → **Resolved** with the curated-matcher extension landed at [`src/lib.rs:330-380`](../../src/lib.rs) covering the eight additional Cf codepoints/ranges. Post-fix cold-verification by the same session confirmed both dispositions held against the post-fix artifacts. Per [G-151](../../../../vsdd-suite/suite-development/FINDINGS-INDEX.md#g-151) stop-trigger discipline, the Round 3 in-round verification record satisfies the equivalent of the typical Round-N+1 confirmation pass; a confirming Round 4 remains available at director discretion but is not strictly required.

The adversarial-probe shape of this round matched the primer's "verify every claimed control by attempting to bypass it" discipline:

- **Load-side symlink rejection was probed with four attack-shape categories** (binding to the symlink at check time; symlink target shape variation; read-oracle compound via `bm add`; TOCTOU race). Three bypass attempts failed (the synchronous control holds); the TOCTOU race surfaces a real new defect class — recorded as Finding 2.
- **Broadened `is_format_char` was probed against R2's four named gaps and the full Unicode Cf category.** All four R2-named gaps are closed. The broader Cf category surfaces eight more uncovered Cf characters/ranges — recorded as Finding 3 (spec-vs-implementation parity recurrence).
- **R2-resolved controls were re-verified for regression** — terminal escape, mode 0600, save-side symlink, deny.toml + CI, env-var trust delegation, URL length, concurrent-write race. All controls hold; no regression introduced by the R3 fix cycle.
- **Hallucinated findings re-verified** — unsafe, integer overflow, panic-as-DoS, JSON depth-bomb. All controls continue to hold; the R3 fix cycle added no defects of these classes.

The Round 2 control progression (4 Resolved + 2 Open → R3 fix cycle → 2 new Resolved (R3 F1 + R3 F5) + 2 new findings (R3 F2 + R3 F3)) matches the standard adversarial-refinement decay pattern: each fix layer closes a synchronous defect and surfaces an asynchronous adjacent defect at the next-finer scale. The TOCTOU finding (F2) and the spec/implementation parity finding (F3) are next-finer-scale recurrences of R2 F5 and R2 F6 respectively; the methodology is operating as designed.

**Coordination:**

- **Finding 2 (load-side TOCTOU)** routes to [Software Engineer](../SOFTWARE-ENGINEER-REVIEW.md) for the `O_NOFOLLOW` open implementation + [Solution Owner](../SOLUTION-OWNER-REVIEW.md) for the DESIGN.md § Threat model amendment naming the TOCTOU closure on load. Also flag to [Security](../SECURITY-REVIEW.md) — this is an absent-control finding the Security domain should ensure is added to the threat-model surface; per the Red-Team-to-Security flag-back convention, if the parallel Security Round 3 does not also surface this, the divergence is its own Security-domain finding.
- **Finding 3 (Cf coverage parity recurrence)** routes to [Solution Owner](../SOLUTION-OWNER-REVIEW.md) for the spec-direction choice (broaden via dependency vs. narrow via enumeration) + [Software Engineer](../SOFTWARE-ENGINEER-REVIEW.md) for the implementation side. Also flag to [Security](../SECURITY-REVIEW.md) — the Rust supplement § Security "Cf format chars" framing is the floor that the spec implements as a subset; the validator pair should align spec wording with implementation at this round's resolution.
- **Findings 4 + 6 + 7 + 8 + 9 + 10 + 11 (re-verified Accepted risk)** need no further coordination — the trust delegations remain documented in DESIGN.md § Threat model + § Scope and non-goals per the Round 1 / Round 2 acceptance.
- **Findings 12 + 13 + 14 + 15 (re-verified Hallucinated)** need no coordination — recorded for audit completeness only. The Round 3 fix cycle added no defects of these classes.

Per the validator-pair convention from the [Red Team domain prompt](../../../../vsdd-suite/domains/role/RED-TEAM-REVIEW.md) § Validator pair (Review 77): Resolved findings declare `Validator: security`; Hallucinated findings with no validation surface declare `Validator: sanity-check` per the meta-validator-of-last-resort pattern. The new Findings 2 and 3 declare `Validator: security` pending the Round 4 cold pass.

---
