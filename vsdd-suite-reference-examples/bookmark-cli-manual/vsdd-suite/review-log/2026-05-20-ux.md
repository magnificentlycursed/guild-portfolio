# UX Designer Review — 2026-05-20

[Index](../UX-REVIEW.md)

---

## Review 1 — 2026-05-20 19:30Z

**Layer:** Layer 1 — Add and List ([`TODO.md` § Layer 1](../../TODO.md#layer-1--add-and-list)).
**Tested against:** Layer 1 close — [Phase 2b](../../../../vsdd-suite/primers/2b-implementation.md) implementation committed; [QE Review 1](2026-05-17-quality-engineer.md), [QE Review 2](2026-05-20-quality-engineer.md#review-2--2026-05-20-0245z), and [SA Review 1](2026-05-20-solution-architect.md#review-1--2026-05-20-0245z) closed; [Phase 5](../../../../vsdd-suite/primers/5-formal-hardening.md) Purity Boundary Audit + Mutation Testing closed at 2026-05-20 02:45Z.
**Round:** 1 (first UX cold-context round against bookmark-cli-manual; no prior UX rounds — see [`../UX-REVIEW.md`](../UX-REVIEW.md) Reviews table).
**Active domain set:** Per [`../../DESIGN.md`](../../DESIGN.md) § Project intent — 11 role + 1 meta domains active at capstone intent (SE, QE, UX, [Security](../../../../vsdd-suite/domains/role/SECURITY-REVIEW.md), SA, SO, [Performance Engineer](../../../../vsdd-suite/domains/role/PERFORMANCE-ENGINEER-REVIEW.md), [Platform Engineer](../../../../vsdd-suite/domains/role/PLATFORM-ENGINEER-REVIEW.md), [Red Team](../../../../vsdd-suite/domains/role/RED-TEAM-REVIEW.md), [Technical Writer](../../../../vsdd-suite/domains/role/TECHNICAL-WRITER-REVIEW.md), [Documentation Reviewer](../../../../vsdd-suite/domains/role/DOCUMENTATION-REVIEWER-REVIEW.md), [VDD-IAR Alignment](../../../../vsdd-suite/domains/meta/VDD-IAR-ALIGNMENT-REVIEW.md)). [Data Engineer](../../../../vsdd-suite/domains/role/DATA-ENGINEER-REVIEW.md) ruled out per [G-178](../../../../vsdd-suite/suite-development/FINDINGS-INDEX.md#g-178).

**Scope:** Whole application at Layer 1 surface — `bm add` and `bm list` user-facing behavior. Artifacts read: [`../../README.md`](../../README.md); [`../../src/main.rs`](../../src/main.rs) (the CLI entry — primary user-facing surface); [`../../src/lib.rs`](../../src/lib.rs); [`../../TODO.md`](../../TODO.md); [`../../manual-tests/layer-1.md`](../../manual-tests/layer-1.md) (primary UX evidence per the CLI supplement); [`../../DESIGN.md`](../../DESIGN.md) (read last per Phase 3 primer discipline). [CLI supplement § UX](../../../../vsdd-suite/supplements/cli.md) dimensions applied in place of the browser-centric defaults per the [UX domain prompt](../../../../vsdd-suite/domains/role/UX-REVIEW.md) § Interface type.

**Lens:** Cold-context adversarial UX pass (no prior participation in the project's build). Applied [CLI supplement § UX](../../../../vsdd-suite/supplements/cli.md) dimensions 1–11.

**Session note:** Cold session — the reviewer has no prior participation in the project's build, no prior loaded context about bookmark-cli, and read the listed artifacts in the prescribed order (README → main.rs → lib.rs → TODO.md → manual-tests/layer-1.md → DESIGN.md last). Per the [UX domain prompt](../../../../vsdd-suite/domains/role/UX-REVIEW.md) § Sycophancy check: an AI agent cannot experience a CLI by running it — the findings below are derived from reading the prescribed source artifacts (executable code, spec, and the user-facing manual test plan). Where a dimension required actually running `bm --help` or `bm list` against a real terminal to judge, the reviewer worked from the literal output specified in the spec / manual test / source code rather than executing the binary. Dimensions whose evaluation depends on lived experience (cf. WCAG-style perceived-latency, color comfort) are not applicable to this CLI surface.

**Source:** `domain-raised` — the [CLI supplement § UX](../../../../vsdd-suite/supplements/cli.md) dimensions applied cold against the source/spec/manual-test artifacts surfaced every finding below.

**Regression check:** No prior UX rounds exist (per [`../UX-REVIEW.md`](../UX-REVIEW.md) Reviews table — "no rounds filed yet"). Adjacent-domain UX-touching findings reviewed for cross-domain regression: [SA Review 1](2026-05-20-solution-architect.md#review-1--2026-05-20-0245z) Finding 1 (purity-boundary divergence; resolved via [DESIGN.md](../../DESIGN.md) § Verification architecture rewrite + [`src/lib.rs:1-?`](../../src/lib.rs) module-doc rewrite) — verified the rewrite did not introduce user-facing language regressions in `src/main.rs` or error messages. [QE Review 2](2026-05-20-quality-engineer.md#review-2--2026-05-20-0245z) Finding 1 (Mutation Testing nested-parent-dir test gap; resolved via new `save_creates_parent_directory_for_nested_path` test) — verified the test addition did not change any user-visible behavior (still silent on success, exit 0).

---

### Deferred

<a id="r1-f1"></a>**Finding 1 — NUL-byte placeholders in user-facing manual test step headings (Dim 3)**

**Owner:** ux
**Status:** raised
**Blocked by:** *(none)*

The primary UX evidence file — [`manual-tests/layer-1.md`](../../manual-tests/layer-1.md) — contains literal NUL bytes (`0x00`) wrapping placeholder tokens in three step headings:

- Line 34: `## Step 1 — Happy path: \x00PROT_30\x00 captures a bookmark`
- Line 93: `## Step 3 — \x00PROT_40\x00 orders newest-first`
- Line 120: `## Step 4 — Empty-state: \x00PROT_46\x00 against an absent store`

(Byte-level confirmed via Python `repr()` of each line.) The `\x00`-wrapped tokens look like incompletely-filled section-title templates — possibly artifacts of a redaction or sanitization sweep that replaced the original titles with placeholder sigils but left them in place rather than completing them.

**Behavior consequence:** A user following the per-layer manual test plan sees the step headings render as either invisible NUL bytes (most markdown viewers + `cat` swallow the byte silently and concatenate the surrounding text into nonsense like `Happy path: PROT_30 captures a bookmark`) or as `^@` / `<00>` glyphs (less, vim, IDE markdown previews). The headings convey no real meaning either way — the placeholder tokens have not been replaced with actual descriptive heading text. The CLI supplement Dim 3 (Output scannability) extends to the manual test rendering because the manual test IS the user-facing UX evidence (per primer 3 § Manual testing-second-adversarial-surface framing in [G-132](../../../../vsdd-suite/suite-development/FINDINGS-INDEX.md#g-132)). Unrenderable step headings degrade scannability to zero for the affected steps.

Adjacent: shells, pagers, and tools that don't NUL-tolerate (some grep configurations, awk, sort) may silently truncate or behave unexpectedly on lines containing NUL — a user who pipes the manual-test file through any such tool will get inconsistent output without an obvious error.

**Classification:** Open — fix is to replace each `\x00PROT_NN\x00` token with a real descriptive heading fragment (e.g., `add` for Step 1; `list` for Step 3; `list` for Step 4) and re-save the file as clean UTF-8. **Validator pair:** [quality-engineer](../../../../vsdd-suite/domains/role/QUALITY-ENGINEER-REVIEW.md) per the [UX domain prompt](../../../../vsdd-suite/domains/role/UX-REVIEW.md) Validator-pair paragraph (behavioral UX defect — manual-test rendering is verifiable by reading the file post-fix; QE can confirm no remaining NUL bytes by file scan).

---

<a id="r1-f3"></a>**Finding 3 — Manual-test "expected on-disk JSON" example contradicts the actual storage format (Dim 6)**

**Owner:** ux
**Status:** raised
**Blocked by:** *(none)*

[`manual-tests/layer-1.md`](../../manual-tests/layer-1.md):59–63 documents the expected `cat "$BOOKMARK_CLI_DB"` output for Step 1 as:

```json
[{"url":"https://example.com","timestamp":"2026-05-20T22:15:42.371Z"}]
```

— a bare JSON array of bookmark objects. The implementation in [`src/lib.rs`](../../src/lib.rs):60–72 (`BookmarkStore::save`) calls `serde_json::to_string_pretty(self)` against `BookmarkStore`, which is `struct BookmarkStore { pub bookmarks: Vec<Bookmark> }` ([`src/lib.rs`](../../src/lib.rs):33–37). The serialized output is therefore a JSON **object** with a `bookmarks` key wrapping the array — confirmed by [`DESIGN.md`](../../DESIGN.md):103–110 § Storage format, which declares the canonical shape as:

```json
{
  "bookmarks": [
    {"url": "https://example.com", "timestamp": "2026-05-17T03:01:00Z"},
    ...
  ]
}
```

— and confirmed against the integration test [`tests/bookmarks.rs`](../../tests/bookmarks.rs):35–37 which parses the on-disk file as `parsed["bookmarks"].as_array()` (key-indexed against the wrapping object). The manual test's "expected" example is therefore wrong on every Layer-1 invocation: a user following Step 1 runs `cat "$BOOKMARK_CLI_DB"` and sees `{"bookmarks":[{"url":"https://example.com","timestamp":"..."}]}` (object-wrapped, pretty-printed across multiple lines via `to_string_pretty`), then compares against the documented expectation `[{"url":...,"timestamp":...}]` and concludes either (a) the manual test is wrong (the actual UX defect), or (b) the binary is broken (a false-positive failure that wastes user time).

**Behavior consequence:** The manual test is supposed to be the second adversarial surface (per [G-132](../../../../vsdd-suite/suite-development/FINDINGS-INDEX.md#g-132)) — a user-facing trip wire that catches divergence between spec/test/impl that automated tests miss. Here the trip wire is itself broken: it triggers on every clean run. A user encountering this will lose trust in the manual-test plan as a whole; the discipline of "literal expected-output blocks" is undermined when those blocks don't match reality.

**Classification:** Open — fix is to replace the bare-array expected output with the object-wrapped shape matching [`DESIGN.md`](../../DESIGN.md) § Storage format, and to make the literal example use the pretty-printed multi-line layout that `to_string_pretty` actually produces (since the manual test purports to show `cat`-output, the pretty-printed multi-line form is what the user will actually see). **Validator pair:** [quality-engineer](../../../../vsdd-suite/domains/role/QUALITY-ENGINEER-REVIEW.md) — QE owns expected-vs-observed discipline and can verify the corrected example against a fresh `cargo run -- add ...` + `cat` invocation.

---

<a id="r1-f4"></a>**Finding 4 — `bm --help` lacks a usage example (Dim 1)**

**Owner:** ux
**Status:** raised
**Blocked by:** *(none)*

The clap derive declaration at [`src/main.rs`](../../src/main.rs):14–16 provides:

```rust
#[command(name = "bm", version, about = "Capture URLs at the terminal; recall them later.")]
```

— `name`, `version`, and a one-line `about`. Clap's default `--help` rendering with only these attributes produces a help screen with the about line, usage summary, and the subcommand list, but **no usage example**. The [CLI supplement § UX](../../../../vsdd-suite/supplements/cli.md) Dim 1 explicitly asks: *"Does the top-level help include a usage example?"* The bookmark-cli README ([`README.md`](../../README.md):27–36) includes runnable examples (`bm add https://example.com`; `bm list`), but a user who installs the binary and runs `bm --help` (the discoverable default) does not see them.

The asymmetry matters because `bm --help` is the in-binary discovery surface — the artifact a user reaches for first when they've installed the tool but haven't read the README. A user who copies the example from `bm add --help` will see only `Usage: bm add <URL>` plus the per-argument `/// The URL to capture. Must be non-empty.` doc-comment ([`src/main.rs`](../../src/main.rs):26) — informative but no copy-pasteable example.

**Behavior consequence:** The CLI's discoverability surface skews toward the README (out-of-binary) and away from `--help` (in-binary). For a single-user tool whose installation step is `cargo install --path .` (READMEs not typically visible after install), users will rely on `--help`. The missing example forces a user to either (a) guess the URL syntax (likely OK — it's `https://example.com`-shaped), (b) read the per-argument doc-comment and infer the call shape, or (c) leave the terminal to find the README. None is catastrophic; all are friction the help text could remove.

**Classification:** Open — fix is to add a clap `long_about` or `after_help` attribute to the top-level `Cli` struct that includes a 2–3-line usage block (`bm add <url>` / `bm list` / `bm --help`). The CLI supplement Dim 1 is the operative floor; the README already supplies the content. **Validator pair:** [software-engineer](../../../../vsdd-suite/domains/role/SOFTWARE-ENGINEER-REVIEW.md) per the [UX domain prompt](../../../../vsdd-suite/domains/role/UX-REVIEW.md) Validator-pair paragraph — UI / interaction code change (clap attribute on the `Cli` struct).

---

<a id="r1-f5"></a>**Finding 5 — Storage error messages omit remediation hints (Dim 8)**

**Owner:** ux
**Status:** raised
**Blocked by:** *(none)*

[CLI supplement § UX](../../../../vsdd-suite/supplements/cli.md) Dim 8 names three required components for an error message: *"what failed, why it failed (if knowable), and what the user should do next."* The bookmark-cli storage errors at [`src/main.rs`](../../src/main.rs):50–52 and 56–58 emit:

```rust
eprintln!("Error: {e:#}");
return ExitCode::from(2);
```

— where `{e:#}` renders the anyhow context chain (e.g., `Error: parsing bookmark store at /Users/.../bookmarks.json: expected value at line 1 column 1`). This satisfies the *what* and *why* components: the user sees that parsing failed at a specific file with a serde-level reason. The *what to do next* component is absent — the user is told their store is corrupt but not told whether to (a) edit the file to fix the JSON, (b) delete it to start fresh, (c) restore from a backup, or (d) file a bug. Same gap applies to read-failure cases (e.g., `Error: reading bookmark store at /path: Permission denied (os error 13)` — no hint that `chmod` or `sudo` might help) and write-failure cases.

Adjacent: the empty-URL error at [`src/main.rs`](../../src/main.rs):45 (`Error: URL cannot be empty.`) is exit-1 user-error territory and is short by design — but it also misses the next-step hint. A "Usage: bm add <url>" follow-up line (or `Try 'bm add --help'`) would close the loop. The DESIGN.md error contract ([`DESIGN.md`](../../DESIGN.md):54–69) requires only `Error: URL cannot be empty.\n` for the empty-URL case — adding a remediation hint would change the literal stderr the integration test asserts ([`tests/bookmarks.rs`](../../tests/bookmarks.rs):59 `stderr("Error: URL cannot be empty.\n")`), and the spec's exit-1 stderr line is therefore SO-controlled. For the storage-error case (exit 2), [`DESIGN.md`](../../DESIGN.md):61 specifies `stderr "Error: <descriptive message>" followed by newline` — the descriptive message is not literal, so a remediation suffix can be added without spec amendment.

**Behavior consequence:** Storage errors are the highest-recovery-friction failure mode for this tool — the user has lost data or has a corrupt file, and a wrong recovery step (e.g., deleting the file when it could have been edited) costs them everything saved so far. A remediation hint is the highest-leverage user-experience improvement in the error surface.

**Classification:** Open — fix is to add a remediation suffix to the storage-error formatting in [`src/main.rs`](../../src/main.rs):50–58 and 64–67, e.g., `eprintln!("Error: {e:#}\n  hint: edit or remove the store file to recover");` (exact wording is a UX choice; the floor is that *some* next-step is named). The empty-URL case is excluded from scope here (spec-pinned). **Validator pair:** [software-engineer](../../../../vsdd-suite/domains/role/SOFTWARE-ENGINEER-REVIEW.md) — UI / interaction code change (error-formatting code in `main.rs`).

---

<a id="r1-f6"></a>**Finding 6 — Manual test Step 5 uses a `cd` path that fails from the post-Step-0 working directory (Dim 4)**

**Owner:** ux
**Status:** raised
**Blocked by:** *(none)*

[`manual-tests/layer-1.md`](../../manual-tests/layer-1.md):13 (Step 0) instructs the user to `cd vsdd-suite-reference-examples/bookmark-cli-manual` — placing them at the project root. The session's cwd is unchanged through Steps 1–4 (no `cd` in any of those steps; `cargo`/`bm`/`mktemp` invocations don't change the shell's cwd). [`manual-tests/layer-1.md`](../../manual-tests/layer-1.md):166 (Step 5) then issues:

```sh
cd vsdd-suite-reference-examples/bookmark-cli-manual
```

— but the user is **already in** `vsdd-suite-reference-examples/bookmark-cli-manual/`. The shell will attempt `cd vsdd-suite-reference-examples/bookmark-cli-manual/vsdd-suite-reference-examples/bookmark-cli-manual` (a non-existent path) and fail with `cd: no such file or directory`. The subsequent `cargo install --path .` then runs from the wrong directory (whichever directory the failed `cd` left the shell in — which is the same `bookmark-cli-manual/`, so the install will actually succeed, masking the divergence). The manual-test author appears to have copy-pasted the Step-0 `cd` line without recognizing the user is no longer at the parent path.

**Behavior consequence:** A user running the manual-test plan literally will see `cd: no such file or directory` printed at Step 5 — an unexpected error in a step the spec claims should produce no output. The user is then forced to decide whether the manual test is wrong or whether `cd` is misbehaving. The literal-expected-output discipline ([`manual-tests/layer-1.md`](../../manual-tests/layer-1.md) preamble) is broken — the spec promises this step has no unexpected output, but the literal commands as written generate exactly one (the failed `cd`). The downstream `cargo install` then succeeds by accident, which is the worst kind of failure: a manual test that "passes" through silent state mismatch.

**Classification:** Open — fix is to remove line 166 (the user is already in the right directory) OR replace it with a relative-up + back path that survives whatever cwd Step 4 left the user in. The simplest fix is deletion. **Validator pair:** [quality-engineer](../../../../vsdd-suite/domains/role/QUALITY-ENGINEER-REVIEW.md) — QE owns runnable-manual-test discipline (the per-step expected-vs-observed contract) and can validate the fix by following Steps 0–5 verbatim.

---

<a id="r1-f7"></a>**Finding 7 — Manual test Step 5 expects `which bm` to output literal "bm not found" — no real shell `which` produces that string (Dim 8)**

**Owner:** ux
**Status:** raised
**Blocked by:** *(none)*

[`manual-tests/layer-1.md`](../../manual-tests/layer-1.md):174–180 documents:

> Expected output for `which bm 2>&1` after uninstall (literal — path may differ slightly by shell):
>
> ```
> bm not found
> ```

— but no major shell's `which` produces the literal string `bm not found`:

- macOS BSD `which` (default `/usr/bin/which`): prints nothing on absent binary, exits 1.
- GNU `which` (default on most Linux): prints `which: no bm in (PATH-list)` or nothing depending on the `--show-tilde` and `--skip-alias` defaults.
- zsh / bash builtin `which` (equivalent to `whence`): prints `bm not found` only in zsh's builtin when the shell is launched without `emulate ksh`; bash builtin prefers `type` semantics and prints `bash: type: bm: not found` or `bm: command not found` via `command -v` paths.

The expected literal `bm not found` is closest to the zsh builtin output, but the manual-test plan does not declare zsh as a prerequisite. The README ([`README.md`](../../README.md):14) declares "macOS or Linux" without specifying the shell. A user on macOS with the default `/bin/bash` or `/bin/zsh` will see a different literal — either silent output (BSD `which`) or `bm not found` (zsh builtin) — and the literal-expected-output discipline breaks again.

**Behavior consequence:** Same shape as Finding 6 — a user following the literal expected-output sees a divergence that may not indicate a real defect, lowering trust in the manual-test plan as the second adversarial surface ([G-132](../../../../vsdd-suite/suite-development/FINDINGS-INDEX.md#g-132)). The CLI supplement Dim 8 ("error messages include what failed, why it failed, what to do next") doesn't directly govern third-party tools like `which`, but the manual-test author has imported `which`'s output into the literal-expected-output contract — at that point its fidelity is the manual test's responsibility.

**Classification:** Open — fix is to either (a) replace the literal expectation with a behavior assertion that survives all shell variants (`which bm` exits non-zero; stdout/stderr content is shell-dependent and not asserted) OR (b) declare a specific shell (`zsh`) as the manual-test prerequisite and use the corresponding literal. Option (a) is more portable; option (b) is more verifiable. **Validator pair:** [quality-engineer](../../../../vsdd-suite/domains/role/QUALITY-ENGINEER-REVIEW.md) — runnable-manual-test fidelity is QE territory.

---

### Resolved

*(none — this is a Round 1 cold pass; no findings resolved in-session.)*

---

### Dismissed

<a id="r1-f8"></a>**Finding 8 — Error label uses `Error:` (Dim 8)**

The bookmark-cli error formatting at [`src/main.rs`](../../src/main.rs):45, 51, 57, 66 uses `Error:` (capital E) as the severity label, where the Rust ecosystem (cargo, rustc, clap, anyhow's own examples) uses lowercase `error:`. The capitalized form is not wrong per any portable CLI convention — many tools (curl, ssh, ssh-keygen) use mixed-case labels — but Rust users specifically may notice the mismatch and read it as non-idiomatic.

**Classification:** Dismissed — [`DESIGN.md`](../../DESIGN.md):60 explicitly pins the empty-URL stderr to `Error: URL cannot be empty.` (capital E), and the storage-error format follows the same pattern for spec-consistency. Changing the casing requires a spec amendment + an integration-test update + cascading manual-test literal-output updates. The cost is real; the user-visible benefit is small (Rust-ecosystem alignment, not a functional improvement). At Layer 1 capstone-but-reference-implementation intent, the spec-pin is binding; a Raised-to-SO classification would be defensible but the SO would almost certainly Backlog or Dismiss given the same cost/benefit calculus. Documenting the dismissal here so future UX reviewers don't re-raise it.

---

<a id="r1-f9"></a>**Finding 9 — `bm list` output line format breaks scannability on URLs containing spaces or newlines (Dim 3)**

The list-rendering at [`src/main.rs`](../../src/main.rs):75 uses `println!("{} {}", bm.timestamp.to_rfc3339(), bm.url)` — a single-space separator between timestamp and URL, no quoting or escaping. A URL containing spaces (rare but possible — `https://example.com/path with spaces` if pre-decoded; or whitespace-only URLs per the spec edge case) would produce ambiguous output: `awk '{print $2}'` would print only `https://example.com/path` and discard the rest. A URL containing literal newlines would render across multiple lines, breaking the "one bookmark per line" invariant ([CLI supplement § UX](../../../../vsdd-suite/supplements/cli.md) Dim 3).

**Classification:** Dismissed — [`DESIGN.md`](../../DESIGN.md):80 explicitly accepts URLs containing newlines as a deliberate edge case: *"URL containing newlines: accepted. May visually break the `bm list` output, which is acceptable for this scope."* [`DESIGN.md`](../../DESIGN.md):74 similarly defers whitespace validation to the user. The spec has made an explicit acceptance decision; raising this as a UX defect would re-litigate a closed spec choice. The dismissal is documented so the rationale persists for future rounds; the consequence (`awk`/`grep` fragility on adversarial URLs) is the spec's burden, not an impl defect.

---

### Deferred

*(none — all findings are Layer 1 in scope; no routing to Layer 2 or 3 required.)*

---

### Raised to SO

<a id="r1-f2"></a>**Finding 2 — NUL-byte placeholders in DESIGN.md behavioral contract section headings (Dim 1)**

**Owner:** solution-owner
**Status:** raised
**Blocked by:** *(none)*

[`DESIGN.md`](../../DESIGN.md) § Behavioral contracts uses NUL-wrapped placeholder tokens in its section headings:

- Line 55: `### \x00PROT_37\x00`
- Line 63: `### \x00PROT_41\x00`

(Byte-level confirmed via Python `repr()`.) The two sections are the spec's authoritative behavioral contracts for `bm add` and `bm list` — they describe the input shape, success output, success side effect, and failure semantics. The section headings should read `### bm add <url>` and `### bm list` (or similar descriptive titles) so a reader of the spec can navigate to and cite the contract. Currently the headings convey no information.

**Behavior consequence:** The spec's own table of contents (any markdown viewer's outline; GitHub's auto-generated heading anchors) is broken — auto-generated anchors will hash the NUL bytes and produce un-link-able URL fragments. Cross-references elsewhere in the suite or this project that target these section headings (none yet exist, but any future reference would have to encode the NUL byte in markdown link syntax) are impossible. A cold reader following the [Phase 3 primer](../../../../vsdd-suite/primers/3-review-session.md)'s "Read DESIGN.md first" obligation reads two unlabeled `###` blocks where they expect "bm add" and "bm list" contracts — exactly the navigational seam IAR Phase 3 is supposed to make easy. The defect surfaces in Finding 1 in the parallel-corrupted manual-test step headings: the same authoring-time mistake appears to span both the spec and the user-facing manual test.

This concern affects the spec (DESIGN.md), which is SO-controlled per the [UX domain prompt](../../../../vsdd-suite/domains/role/UX-REVIEW.md) § DESIGN.md change authority. Raised to SO for the fix.

**Classification:** Raised to SO. Proposed change: replace `### \x00PROT_37\x00` (line 55) with `### bm add <url>` and `### \x00PROT_41\x00` (line 63) with `### bm list`; confirm no other NUL-wrapped placeholders elsewhere in the spec (a project-wide byte scan recommended at fix time). Rationale: the two corrupted headings are the spec's behavioral contract anchors for the two Layer 1 commands; readable headings are a discoverability floor for the spec itself. Routing target: a [Solution Owner](../../../../vsdd-suite/domains/role/SOLUTION-OWNER-REVIEW.md) round will apply the fix and the corresponding SO log entry will track resolution.

---

### Hallucinated

*(none — every finding above cites file:line and a specific observed-vs-expected divergence; the reviewer did not invent any failure mode without verifiable evidence.)*

---

### Summary

9 findings filed in Round 1, distributed as: 6 Open (Findings 1, 3, 4, 5, 6, 7), 1 Raised to SO (Finding 2 — DESIGN.md NUL-byte spec-heading corruption), 2 Dismissed with rationale (Findings 8, 9 — spec-pinned error label casing; spec-accepted URL-format edge case), 0 Resolved, 0 Deferred, 0 Hallucinated. The round produced real findings (manual-test corruption — NUL bytes in step headings; manual-test divergence from on-disk JSON shape; missing `--help` example; missing storage-error remediation hints; two manual-test fidelity bugs — Step 5 `cd` and Step 5 `which` literal) — Round 2 is mandatory per the [Phase 3 primer](../../../../vsdd-suite/primers/3-review-session.md) § Continue trigger ([G-131](../../../../vsdd-suite/suite-development/FINDINGS-INDEX.md#g-131)). The Round-2 trigger is the standard "Round N produced new real findings" path, not the stop-trigger MVR path.

**Coordination:** Finding 2 routes to a [Solution Owner](../../../../vsdd-suite/domains/role/SOLUTION-OWNER-REVIEW.md) round (DESIGN.md spec heading corruption requires SO change authority). Findings 1, 3, 6, 7 (manual-test corruption / divergence / fidelity) share QE as the natural validator — coordinate with the [Quality Engineer](../../../../vsdd-suite/domains/role/QUALITY-ENGINEER-REVIEW.md) log at next QE round so the manual-test fixes ride through QE's runnable-manual-test-discipline pass. Findings 4 + 5 (help-text + error-message code changes in `src/main.rs`) route to the [Software Engineer](../../../../vsdd-suite/domains/role/SOFTWARE-ENGINEER-REVIEW.md) review for code-change validation. No cross-domain implications to [Security](../../../../vsdd-suite/domains/role/SECURITY-REVIEW.md), [Performance Engineer](../../../../vsdd-suite/domains/role/PERFORMANCE-ENGINEER-REVIEW.md), or [Platform Engineer](../../../../vsdd-suite/domains/role/PLATFORM-ENGINEER-REVIEW.md) surfaces — the findings are confined to user-facing surface (help text, error formatting, manual-test rendering) and the spec-side heading corruption. The NUL bytes in DESIGN.md (Finding 2) and manual-tests/layer-1.md (Finding 1) likely share a common upstream cause (a sanitization/redaction sweep that left placeholder sigils) — flagging cross-domain to [Technical Writer](../../../../vsdd-suite/domains/role/TECHNICAL-WRITER-REVIEW.md) for project-wide byte-scan at next TW round, since other authoring artifacts may carry the same residue.

---

## Review 2 — 2026-05-20 21:00Z

**Layer:** Layer 1 — Add and List ([`TODO.md` § Layer 1](../../TODO.md#layer-1--add-and-list)).
**Tested against:** Post-Round-1 fix cycle (per [`../../CHANGELOG.md`](../../CHANGELOG.md) 2026-05-20 entries citing [UX Review 1 Findings 1, 2, 3, 6, 7](2026-05-20-ux.md) + the [`../../PROCESS.md`](../../PROCESS.md) "Round 1 NUL-byte sentinel artifacts" note); [Round 1 entry](2026-05-20-ux.md#review-1--2026-05-20-1930z) (9 findings filed; 6 Open + 1 Raised to SO + 2 Dismissed + 0 Resolved + 0 Hallucinated).
**Round:** 2 (Round 1 produced 7 new real findings — F1, F2, F3, F4, F5, F6, F7 — fulfilling the [Phase 3 primer](../../../../vsdd-suite/primers/3-review-session.md) § Continue trigger ([G-131](../../../../vsdd-suite/suite-development/FINDINGS-INDEX.md#g-131)) "any new real findings → Round N+1 mandatory" path).
**Active domain set:** Unchanged from [Round 1](2026-05-20-ux.md#review-1--2026-05-20-1930z) — 11 role + 1 meta active at capstone intent per [`../../DESIGN.md`](../../DESIGN.md) § Project intent.

**Scope:** Whole application at Layer 1 surface — verify each Round 1 finding's resolution (or dismissal-still-holds) against the current artifact state; look for new findings the fix cycle may have introduced (CLI supplement § UX regression-check). Artifacts read: [`../../README.md`](../../README.md); [`../../src/main.rs`](../../src/main.rs); [`../../src/lib.rs`](../../src/lib.rs); [`../../manual-tests/layer-1.md`](../../manual-tests/layer-1.md); [`../../DESIGN.md`](../../DESIGN.md) (read last per Phase 3 primer discipline); plus the [Round 1 entry](2026-05-20-ux.md) for finding-by-finding verification. [CLI supplement § UX](../../../../vsdd-suite/supplements/cli.md) dimensions applied as in Round 1.

**Lens:** Cold-context adversarial UX Round 2 — verify Resolved/Open/Dismissed/Hallucinated for each [Round 1 finding](2026-05-20-ux.md) + look for fix-cycle-introduced new findings. Per the [Phase 3 primer](../../../../vsdd-suite/primers/3-review-session.md) § Continue trigger framing: Round 2's job is to (a) confirm that fixes hold without regression, (b) catch adjacent defects the fix may have created, (c) re-raise findings dismissed without adequate rationale, and (d) test the rationale of the dismissals.

**Session note:** Cold session — this AI session did not participate in the Round 1 fix cycle. Per the [UX domain prompt](../../../../vsdd-suite/domains/role/UX-REVIEW.md) § Sycophancy check: an AI agent cannot experience a CLI by running it; this review works from the literal output specified in the spec / manual test / source code rather than executing the binary. Sycophancy-compensation: every "Resolved" classification below cites the specific artifact change (file:line) that closes the Round 1 evidence — not "the developer says it's fixed." Two findings (F4, F5) are explicitly NOT resolved by the Round 1 fix cycle — the relevant code paths in [`../../src/main.rs`](../../src/main.rs) carry no change against Round 1's cited evidence; classifying them Resolved would be the sycophancy failure mode the primer warns against.

**Source:** `domain-raised` — the same [CLI supplement § UX](../../../../vsdd-suite/supplements/cli.md) dimensions that produced Round 1 surfaced this round's verifications + the new finding (F10) below; no director-raised interrupts.

**Regression check:** [Round 1](2026-05-20-ux.md) findings F1, F3, F6, F7 (manual-test fidelity) are verified against the current [`../../manual-tests/layer-1.md`](../../manual-tests/layer-1.md); F2 (DESIGN.md NUL bytes) against [`../../DESIGN.md`](../../DESIGN.md):55, 64; F4, F5 (CLI binary surface) against [`../../src/main.rs`](../../src/main.rs):28-32, 95, 103, 107, 116. The fix cycle touched additional surface (new Step 0 + Step 6 in manual-tests, atomic-write + symlink-rejection + mode-0600 in src/lib.rs, display_safe sanitizer in src/lib.rs) — those new surfaces are inspected for UX-introduced regressions and one new finding (F10) is filed against Step 0's `which bm` literal-expected-output rendering.

---

### Resolved

<a id="r2-f1"></a>**Finding 1 — NUL-byte placeholders in user-facing manual test step headings (Dim 3) (Round 1 F1 verification)**

**Owner:** ux
**Status:** validated
**Blocked by:** *(none)*
**Validator:** quality-engineer

[Round 1 Finding 1](2026-05-20-ux.md#r1-f1) raised three NUL-wrapped placeholders in [`../../manual-tests/layer-1.md`](../../manual-tests/layer-1.md) step headings (lines 34, 93, 120 at Round 1's filing time). Current state of the same file:

- [`../../manual-tests/layer-1.md`](../../manual-tests/layer-1.md):36 — `## Step 1 — Happy path: \`bm add <url>\` captures a bookmark` (real heading; backtick-delimited command surface; no NUL byte).
- [`../../manual-tests/layer-1.md`](../../manual-tests/layer-1.md):102 — `## Step 3 — \`bm list\` orders newest-first` (real heading; clean UTF-8).
- [`../../manual-tests/layer-1.md`](../../manual-tests/layer-1.md):129 — `## Step 4 — Empty-state: \`bm list\` against an absent store` (real heading; clean UTF-8).

The placeholder tokens have been replaced with descriptive command-surface phrasing. The fix preserves the per-step structure (each Step heading still names the operation being tested) and improves on Round 1's proposed remediation by quoting the command literally in backticks (better scannability for code-shape readers than the prose-only "add"/"list" Round 1 suggested). The CHANGELOG entry at line 30 [`../../CHANGELOG.md`](../../CHANGELOG.md) records the cross-domain fix coverage ([Technical Writer Review 1 Finding 1](2026-05-20-technical-writer.md), [UX Review 1 Finding 1 + 2](2026-05-20-ux.md)) and the PROCESS note at [`../../PROCESS.md`](../../PROCESS.md):56 documents the upstream tooling-pipeline sanitization-sweep root cause.

**Resolution:** Round 1 F1 closed — NUL-byte placeholders replaced with descriptive command-surface headings at [`../../manual-tests/layer-1.md`](../../manual-tests/layer-1.md):36, 102, 129; CHANGELOG records the cross-domain coverage; PROCESS captures the root-cause analysis. Scannability of the manual-test plan restored.

---

<a id="r2-f2"></a>**Finding 2 — NUL-byte placeholders in DESIGN.md behavioral contract section headings (Dim 1) (Round 1 F2 verification)**

**Owner:** solution-owner
**Status:** validated
**Blocked by:** *(none)*
**Validator:** sanity-check

[Round 1 Finding 2](2026-05-20-ux.md#r1-f2) was Raised to SO with proposed change: replace `### \x00PROT_37\x00` (Round-1 line 55) with `### bm add <url>` and `### \x00PROT_41\x00` (Round-1 line 63) with `### bm list`. Current state of the same file:

- [`../../DESIGN.md`](../../DESIGN.md):55 — `### \`bm add <url>\`` (real H3 heading; clean UTF-8; backtick-delimited command surface).
- [`../../DESIGN.md`](../../DESIGN.md):64 — `### \`bm list\`` (real H3 heading; clean UTF-8; backtick-delimited).

Both headings match the proposed change (with the backtick refinement that makes the command-surface rendering more scannable). Spec navigation is restored — the two behavioral-contract sections now carry self-describing anchors that GitHub's auto-anchor will hash to clean URL fragments (`#bm-add-url` / `#bm-list`). Cross-references in other artifacts that target these sections (the section is cited by the SO compliance table at [`2026-05-20-solution-owner.md`](2026-05-20-solution-owner.md):27, 30) now resolve through readable anchors. The same parallel-fix pattern (UX F1 in manual-tests + UX F2 in DESIGN.md, both NUL-byte residue from the same upstream sanitization sweep) is closed in a single SO touch.

**Resolution:** Round 1 F2 closed by SO change — [`../../DESIGN.md`](../../DESIGN.md):55, 64 now carry descriptive `### \`bm add <url>\`` / `### \`bm list\`` headings. The SO-controlled spec change was applied per the [UX domain prompt](../../../../vsdd-suite/domains/role/UX-REVIEW.md) § DESIGN.md change authority; recorded across [`../../CHANGELOG.md`](../../CHANGELOG.md) + [`../../PROCESS.md`](../../PROCESS.md):56. Validator: sanity-check (no natural cross-domain UX/SO pair for spec-heading clarity in the lifecycle taxonomy — Sanity Check confirms the heading rename coheres with DESIGN.md § Behavioral contracts navigation).

---

<a id="r2-f3"></a>**Finding 3 — Manual-test "expected on-disk JSON" example contradicts the actual storage format (Dim 6) (Round 1 F3 verification)**

**Owner:** ux
**Status:** validated
**Blocked by:** *(none)*
**Validator:** quality-engineer

[Round 1 Finding 3](2026-05-20-ux.md#r1-f3) raised that [`../../manual-tests/layer-1.md`](../../manual-tests/layer-1.md):59–63 (Round-1 line numbers) declared the expected `cat "$BOOKMARK_CLI_DB"` output as a bare JSON array `[{"url":...,"timestamp":...}]` while the implementation emits the object-wrapped pretty-printed form `{"bookmarks": [...]}`. Current state of the same file:

- [`../../manual-tests/layer-1.md`](../../manual-tests/layer-1.md):61–72 — expected `cat "$BOOKMARK_CLI_DB"` block now declares: *"a pretty-printed JSON object with one bookmark in the `bookmarks` array per [`DESIGN.md`](../DESIGN.md) § Storage format"* and shows the literal multi-line pretty-printed shape:

  ```json
  {
    "bookmarks": [
      {
        "url": "https://example.com",
        "timestamp": "2026-05-20T22:15:42.371Z"
      }
    ]
  }
  ```

- [`../../manual-tests/layer-1.md`](../../manual-tests/layer-1.md):74 — invariant prose now correctly names "a JSON object with a `bookmarks` field whose value is an array; the array has exactly one object; the object has fields `url` ... and `timestamp` ...".

The fix matches Round 1's preferred resolution (object-wrapped shape matching [`../../DESIGN.md`](../../DESIGN.md):103-110 + the pretty-printed multi-line layout `to_string_pretty` actually produces). The CHANGELOG entry at [`../../CHANGELOG.md`](../../CHANGELOG.md):34 records the fix and the cross-domain coverage with [Solution Owner Review 1 Finding 2](2026-05-20-solution-owner.md#r1-f2).

**Resolution:** Round 1 F3 closed — manual-test Step 1 expected-output block at [`../../manual-tests/layer-1.md`](../../manual-tests/layer-1.md):61-72 now declares the object-wrapped pretty-printed shape matching [`../../DESIGN.md`](../../DESIGN.md):103-110. The "literal expected output" discipline is restored: a user running Step 1 verbatim sees output matching the documented expectation byte-for-byte (modulo the timestamp invariant explicitly named in the prose).

---

<a id="r2-f6"></a>**Finding 6 — Manual test Step 5 uses a `cd` path that fails from the post-Step-0 working directory (Dim 4) (Round 1 F6 verification)**

**Owner:** ux
**Status:** validated
**Blocked by:** *(none)*
**Validator:** quality-engineer

[Round 1 Finding 6](2026-05-20-ux.md#r1-f6) raised that Step 5's `cd vsdd-suite-reference-examples/bookmark-cli-manual` at Round-1 line 166 would fail because the user was already at that directory after Step 0. Current state of the same file:

- [`../../manual-tests/layer-1.md`](../../manual-tests/layer-1.md):167-169 — Step 5 now captures the project directory as an absolute path BEFORE the `cargo uninstall` step:

  ```sh
  # Record the project directory as an absolute path before uninstall
  # (so the reinstall step does not depend on session cwd).
  PROJECT_DIR="$(pwd)"
  ```

- [`../../manual-tests/layer-1.md`](../../manual-tests/layer-1.md):178-179 — the reinstall step uses the captured absolute path:

  ```sh
  # Reinstall — use the absolute path captured above
  cd "$PROJECT_DIR"
  cargo install --locked --path . --force --quiet
  ```

The fix robustifies Step 5 beyond Round 1's "simplest fix is deletion" proposal — capturing `PROJECT_DIR` makes the step survive whatever cwd state Step 4 leaves the shell in (and is independent of whether the user's session ever changed directories away from the project root). The session-state preamble at [`../../manual-tests/layer-1.md`](../../manual-tests/layer-1.md):8 explicitly names the "single uninterrupted shell session" expectation that supports this design. The CHANGELOG entry at [`../../CHANGELOG.md`](../../CHANGELOG.md):34 records the fix.

**Resolution:** Round 1 F6 closed — Step 5's failure-mode-on-literal-execution removed. The `$PROJECT_DIR="$(pwd)"` snapshot at [`../../manual-tests/layer-1.md`](../../manual-tests/layer-1.md):169 followed by `cd "$PROJECT_DIR"` at line 179 makes the reinstall path absolute and survives session cwd drift. The literal-expected-output discipline is restored for Step 5.

---

<a id="r2-f7"></a>**Finding 7 — Manual test Step 5 expects `which bm` to output literal "bm not found" — no real shell `which` produces that string (Dim 8) (Round 1 F7 verification)**

**Owner:** ux
**Status:** validated
**Blocked by:** *(none)*
**Validator:** quality-engineer

[Round 1 Finding 7](2026-05-20-ux.md#r1-f7) raised that the Round-1 expected-literal `bm not found` at Round-1 lines 174-180 was a zsh-specific output and would diverge on bash / BSD `which` / GNU `which`. Current state of the same file:

- [`../../manual-tests/layer-1.md`](../../manual-tests/layer-1.md):187 — the expectation is now phrased as a behavioral assertion rather than a literal-output expectation:

  > Expected behavior for `which bm 2>&1` after uninstall: the `which bm` command MUST exit non-zero AND MUST NOT print a path. The exact textual output is shell-dependent (bash: typically empty stdout/stderr + exit 1; zsh: may print `bm not found` + exit 1; BSD `which` on macOS: typically empty + exit 1); do not assert on it.

This matches Round 1's recommended option (a): "replace the literal expectation with a behavior assertion that survives all shell variants (`which bm` exits non-zero; stdout/stderr content is shell-dependent and not asserted)." The acceptance criterion is now testable across all three shell variants Round 1 enumerated — no shell-prerequisite declaration was needed (which would have been Round 1's option (b)). The follow-up exit-code expectation at [`../../manual-tests/layer-1.md`](../../manual-tests/layer-1.md):189-193 still asserts the literal `uninstall-exit: 1` which is portable across all three shells (it's `which`'s exit code via `echo "uninstall-exit: $?"`). The CHANGELOG entry at [`../../CHANGELOG.md`](../../CHANGELOG.md):34 records cross-domain coverage with [Documentation Reviewer Review 1 Finding 11](2026-05-20-documentation-reviewer.md).

**Resolution:** Round 1 F7 closed — Step 5's `which bm` post-uninstall expectation rewritten as a portable behavioral assertion at [`../../manual-tests/layer-1.md`](../../manual-tests/layer-1.md):187. The literal-expected-output discipline is preserved for the parts that ARE shell-portable (`uninstall-exit: 1`) and explicitly relaxed for the part that isn't (the textual content of `which`'s output).

---

### Dismissed

<a id="r2-f4"></a>**Finding 4 — `bm --help` lacks a usage example (Dim 1) (Round 1 F4 verification)**

[Round 1 Finding 4](2026-05-20-ux.md#r1-f4) raised that the clap derive declaration at [`../../src/main.rs`](../../src/main.rs):14-16 (Round-1 line numbers) provided only `name`, `version`, and a one-line `about`, producing a `--help` screen with no usage example. The proposed fix was to add a clap `long_about` or `after_help` attribute with a 2–3-line usage block.

Current state of the relevant code:

- [`../../src/main.rs`](../../src/main.rs):27-32 — the clap derive declaration is structurally unchanged against Round 1:

  ```rust
  #[derive(Parser)]
  #[command(
      name = "bm",
      version,
      about = "Capture URLs at the terminal; recall them later."
  )]
  struct Cli {
  ```

  No `long_about`, no `after_help`, no `arg_required_else_help`, no usage-example string anywhere in the `#[command(...)]` block. The clap derive attribute set on the top-level `Cli` struct produces the same minimal help screen Round 1 flagged.

The CHANGELOG (audited via `grep` for `long_about|after_help|usage example|--help` across [`../../CHANGELOG.md`](../../CHANGELOG.md) and [`../../PROCESS.md`](../../PROCESS.md)) carries no entry for adding a usage example. The SE Review 1 log at [`2026-05-20-software-engineer.md`](2026-05-20-software-engineer.md) — the natural validator pair for this finding per [Round 1 F4](2026-05-20-ux.md#r1-f4)'s validator-pair declaration — covers 5 SE-owned findings + 1 Raised-to-SO; none of them touch the clap help-text surface. The Round 1 fix cycle did not address this finding.

**Classification:** Re-raised as Open — the Round 1 fix cycle resolved 5 of 7 Open findings (F1, F2, F3, F6, F7) but did not address F4. The fix's absence is not rationalized as intentional anywhere in the project artifacts (no DESIGN.md amendment, no CHANGELOG dismissal, no SE-log finding). Per the [Phase 3 primer](../../../../vsdd-suite/primers/3-review-session.md) § Sycophancy guard: "A finding that is real but uncomfortable is more valuable than a clean pass that misses a defect." The Round 1 evidence (no usage example in `bm --help`) holds against the current artifact state; this is a real surviving defect, not a fix-cycle oversight that an adversary should soften.

This Round-2 entry is filed under **Dismissed** ONLY in the structural sense that the per-domain log requires a classification heading for every finding raised; the substantive classification is "Open — re-raised for Round 3 fix cycle." Per the [UX domain prompt](../../../../vsdd-suite/domains/role/UX-REVIEW.md) Validator-pair paragraph, the validator pair remains [software-engineer](../../../../vsdd-suite/domains/role/SOFTWARE-ENGINEER-REVIEW.md) — UI / interaction code change (clap attribute on the `Cli` struct). Recommended fix per Round 1: add `long_about` or `after_help` to the `#[command(...)]` block with a 2–3-line usage block (`bm add <url>` / `bm list` / `bm --help`).

**Director note:** structurally classifying a surviving real finding as "Dismissed" would misrepresent the project state. The per-domain log convention's classification universe (Resolved / Deferred / Dismissed / Hallucinated) does not include a natural "Re-raised Open" classification for Round 2's re-verification of an unfixed Round 1 finding. This entry uses Dismissed-by-structure with the substantive Open-re-raise narrative inline; the human classifier should re-classify as the project's intended forward-only Open if the per-project-review hook supports it, or treat this Dismissed-with-rationale as the audit-trail equivalent. Per the [primer](../../../../vsdd-suite/primers/3-review-session.md) § After each domain review classification universe: Deferred is reserved for routing to a specific future layer — F4 is not routed to a future layer; it's a Layer-1-in-scope defect awaiting a Round-3 fix cycle.

---

<a id="r2-f5"></a>**Finding 5 — Storage error messages omit remediation hints (Dim 8) (Round 1 F5 verification)**

[Round 1 Finding 5](2026-05-20-ux.md#r1-f5) raised that storage-error formatting at [`../../src/main.rs`](../../src/main.rs):50-52 and 56-58 (Round-1 line numbers) emits the anyhow context chain without a remediation hint, missing CLI supplement Dim 8's "what to do next" requirement. The proposed fix was to add a remediation suffix (e.g., `\n  hint: edit or remove the store file to recover`) to the storage-error formatting in `main.rs` at the four cited error sites (excluding the empty-URL case which is spec-pinned).

Current state of the relevant code:

- [`../../src/main.rs`](../../src/main.rs):95 — `eprintln!("Error: {}", display_safe(&format!("{e:#}")));` for `bm add` load failure.
- [`../../src/main.rs`](../../src/main.rs):103 — same pattern for `bm add` library-invariant rejection.
- [`../../src/main.rs`](../../src/main.rs):107 — same pattern for `bm add` save failure.
- [`../../src/main.rs`](../../src/main.rs):116 — same pattern for `bm list` load failure.

The Round 1 fix cycle DID modify these lines — they now wrap the anyhow error chain in `display_safe()` for terminal-escape-sequence sanitization (per [Security Review 1 Finding 1](2026-05-20-security.md#r1-f1) + [Red Team Review 1 Finding 4](2026-05-20-red-team.md) — confirmed by the module-doc comment at [`../../src/main.rs`](../../src/main.rs):10-16). But the *remediation hint* component Round 1 F5 raised is still absent. No `hint:` line, no `Try '...' for help.` suffix, no "edit or remove the store file to recover" string anywhere in the error-formatting sites.

The CHANGELOG (audited via `grep` for `remediation|hint` across [`../../CHANGELOG.md`](../../CHANGELOG.md) and [`../../PROCESS.md`](../../PROCESS.md)) carries no entry for adding remediation hints. The SE Review 1 log covers 5 SE-owned findings + 1 Raised-to-SO; none of them address the storage-error remediation-hint surface. The Round 1 fix cycle addressed the Security/Red-Team sanitization concern at the same error sites but did NOT address the UX remediation-hint concern.

**Classification:** Re-raised as Open — the Round 1 fix cycle modified the cited code but did not address Round 1 F5's remediation-hint floor. The Round 1 evidence (CLI supplement Dim 8 requires "what to do next" alongside "what failed" and "why it failed"; the current error messages still carry only "what" + "why" via the anyhow context chain) holds against the current artifact state. The fix's absence is not rationalized as intentional anywhere in the project artifacts (no DESIGN.md amendment declaring "remediation hints are out of scope at Layer 1"; no CHANGELOG entry dismissing F5).

Per the Round 1 entry, the empty-URL case (exit-1 user error) is spec-pinned and excluded from the remediation-hint scope. F5 applies only to the storage-error exit-2 cases at lines 95, 107, 116 (and the library-invariant rejection at line 103, which forwards an anyhow message that ultimately traces to the same empty-URL contract — Round 2 leaves the empty-URL pinning unchanged).

Same structural-classification caveat as F4 above: this Round-2 entry is filed under **Dismissed** for log-structure compliance but the substantive classification is "Open — re-raised for Round 3 fix cycle." Per the [UX domain prompt](../../../../vsdd-suite/domains/role/UX-REVIEW.md) Validator-pair paragraph, the validator pair remains [software-engineer](../../../../vsdd-suite/domains/role/SOFTWARE-ENGINEER-REVIEW.md) — UI / interaction code change (error-formatting code at the four cited storage-error sites in `main.rs`).

**Director note:** identical structural concern to F4 — the per-domain log convention's classification universe forces a binary choice between Dismissed-with-rationale and the substantively-correct re-raise. The CHANGELOG does not commit the project to a Layer-2 deferral of the remediation-hint surface; the natural forward-only path is a Round-3 fix-cycle that adds the hint suffix at the four cited storage-error sites.

---

<a id="r2-f8"></a>**Finding 8 — Error label uses `Error:` (Dim 8) (Round 1 F8 verification)**

[Round 1 Finding 8](2026-05-20-ux.md#r1-f8) was Dismissed with rationale: [`../../DESIGN.md`](../../DESIGN.md):60 pins the empty-URL stderr to `Error: URL cannot be empty.` (capital E), and the storage-error format follows the same pattern for spec-consistency.

Current state confirms the dismissal still holds:

- [`../../DESIGN.md`](../../DESIGN.md):60 — the spec still pins the empty-URL stderr literal `Error: URL cannot be empty.` (capital E).
- [`../../src/main.rs`](../../src/main.rs):68, 89, 95, 103, 107, 116 — the implementation still uses `Error:` (capital E) at every error site, consistent with the spec pin.

No spec amendment changed the casing; no Round 1 fix touched this surface. The cost/benefit calculus Round 1 cited (spec amendment + integration-test update + cascading manual-test updates vs. Rust-ecosystem alignment with no functional improvement) is unchanged.

**Classification:** Dismissed — Round 1's dismissal rationale holds without modification at Round 2. The control (spec-pinned casing) is intact; the concern (Rust-ecosystem mixed-case mismatch) is unchanged; no new evidence that would re-open the dismissal has surfaced.

---

<a id="r2-f9"></a>**Finding 9 — `bm list` output line format breaks scannability on URLs containing spaces or newlines (Dim 3) (Round 1 F9 verification)**

[Round 1 Finding 9](2026-05-20-ux.md#r1-f9) was Dismissed with rationale: [`../../DESIGN.md`](../../DESIGN.md):80 explicitly accepts URLs containing newlines as a deliberate edge case ("URL containing newlines: accepted. May visually break the `bm list` output, which is acceptable for this scope."), and [`../../DESIGN.md`](../../DESIGN.md):74 defers whitespace validation to the user.

Current state confirms the dismissal still holds — with one fix-cycle-introduced refinement that strengthens the original dismissal:

- [`../../DESIGN.md`](../../DESIGN.md):81 — the URL-with-newlines acceptance is unchanged (the edge-case catalog still names it as accepted).
- [`../../DESIGN.md`](../../DESIGN.md):75 — whitespace-only URL acceptance is unchanged.
- [`../../src/main.rs`](../../src/main.rs):125 — the list-output formatting is now `println!("{} {}", bm.timestamp().to_rfc3339(), display_safe(bm.url()));`. The `display_safe()` wrapper introduced in the Round 1 fix cycle for [Security Review 1 Finding 1](2026-05-20-security.md#r1-f1) escapes terminal-control / format characters but explicitly preserves `\n` and `\t` per [`../../src/lib.rs`](../../src/lib.rs):282-284. The "URLs containing newlines may visually break `bm list`" spec acceptance therefore still holds — newlines pass through unescaped — and the bidi-spoof / ANSI-escape concern Round 1 did NOT raise but is now correctly closed via display_safe.

The Round-1 dismissal's specific concerns (whitespace and newline breaking scannability) are still in-scope for the spec's acceptance; the unrelated terminal-escape concern that *could* have been raised under the same Dim is now defended by the display_safe sanitizer. No re-litigation of the closed spec choice is warranted.

**Classification:** Dismissed — Round 1's dismissal rationale holds. The control (DESIGN.md's explicit spec acceptance of the edge case) is intact; the additional terminal-escape defence (display_safe) strengthens the user-visible output discipline without re-opening the dismissed scope.

---

### Deferred

*(none — every Round 1 finding's current state is verified above; no findings are being routed to a later layer in this round.)*

---

### Hallucinated

*(none — every Round 1 finding cited specific file:line evidence; the Round 2 verification confirms each is either Resolved by a specific artifact change (F1, F2, F3, F6, F7) or is a real unfixed defect (F4, F5) or holds a valid Round 1 dismissal (F8, F9). No Round 1 finding has been demonstrated to be a non-defect.)*

---

### Deferred

<a id="r2-f10"></a>**Finding 10 — Manual test Step 0 expected `which bm` output uses literal `~/.cargo/bin/bm` but `which` does not tilde-expand on output (Dim 8 — fix-cycle-introduced)**

**Owner:** ux
**Status:** raised
**Blocked by:** *(none)*

The fix cycle added a new Step 0 (binary install) to [`../../manual-tests/layer-1.md`](../../manual-tests/layer-1.md):12-32 — a sibling of the now-rewritten Step 5 `which` block (Round 1 F7). Step 0's expected output for `which bm` at line 22-24 is:

```
~/.cargo/bin/bm
```

— with the prose note at line 20 acknowledging "path varies by user; the invariant is that the binary resolves under the user's cargo home" and at line 26 noting the cargo-home default of `~/.cargo/bin`.

The problem: `which` does NOT tilde-expand its output. A user running Step 0 verbatim on macOS or Linux will see the literal expansion of `~/.cargo/bin/bm` — typically `/Users/<username>/.cargo/bin/bm` on macOS or `/home/<username>/.cargo/bin/bm` on Linux. The literal expectation block at line 22-24 shows `~/.cargo/bin/bm` which is never the actual `which` output. This is the same fidelity defect as Round 1 F7 (literal-expected-output discipline broken by a shell-output mismatch), in a different step.

The prose note at line 20 partially compensates ("path varies by user; the invariant is that the binary resolves under the user's cargo home") — but the literal block at line 22-24 still presents `~/.cargo/bin/bm` *as if it were a literal expected output*, which conflicts with the manual-test plan's stated literal-expected-output discipline (the same discipline that Round 1 F1, F3, F6, F7 found broken in other ways and that the fix cycle restored elsewhere).

**Behavior consequence:** A literal-expected-output reader (the audience the manual-test plan targets per the [G-132](../../../../vsdd-suite/suite-development/FINDINGS-INDEX.md#g-132) second-adversarial-surface framing) sees `which bm` print `/Users/alice/.cargo/bin/bm` and compares against the documented `~/.cargo/bin/bm`. They either conclude (a) the manual test is wrong (a UX defect), (b) the install is wrong (a false-positive failure), or (c) they read the prose note and mentally substitute the actual home path for the tilde — at which point the literal block has become a sketch rather than a literal, which is the documented discipline broken. The defect is identical in shape to Round 1 F7 (literal-expected-output discipline broken by a shell-output mismatch), and the fix cycle's introduction of Step 0 has re-introduced the same class of defect Round 1 closed for Step 5.

The CLI supplement [§ UX](../../../../vsdd-suite/supplements/cli.md) Dim 8 doesn't directly govern third-party tool output (`which` is not bookmark-cli's own output), but the manual-test author has imported `which`'s output into the literal-expected-output contract — at that point its fidelity is the manual test's responsibility, exactly as Round 1 F7 framed.

**Classification:** Open — fix is to either (a) replace the literal `~/.cargo/bin/bm` with a behavioral assertion ("output is a path ending in `/.cargo/bin/bm`; exact prefix varies by `$HOME`"), or (b) replace the literal with a regex-shape contract (`/.*\.cargo/bin/bm/`), or (c) parameterize the literal as `"$HOME/.cargo/bin/bm"` and document that the user should compare against the expansion of that shell expression (not the tilde-literal). Option (a) matches the Round 1 F7 resolution and is consistent with the manual-test plan's broader convention. Validator pair: [quality-engineer](../../../../vsdd-suite/domains/role/QUALITY-ENGINEER-REVIEW.md) — runnable-manual-test fidelity is QE territory (same as Round 1 F7's validator pair).

**Cross-finding consistency:** Round 1 F7 was Resolved by relaxing the literal-output assertion for `which bm` to a behavioral assertion in Step 5; Step 0's new `which bm` block re-introduces a literal-output assertion using `~`-prefixed path notation that `which` does not produce. The fix-cycle pattern of "Step 5 fixed; Step 0 carries the same shape of defect" is the canonical example of fix-cycle-introduced adjacent defect that the [Phase 3 primer](../../../../vsdd-suite/primers/3-review-session.md) § Continue trigger explicitly names: "the Round N+1 cold pass verifies the fix held and looks for adjacent defects the fix may have created."

---

### Raised to SO

*(none — F4 and F5 are SE-domain code-change findings, not spec-amendment findings. F10's three resolution options are all manual-test-plan edits, not spec changes. No spec amendment proposed by this round.)*

---

### Summary

10 findings inspected in Round 2: **5 Resolved** (F1, F2, F3, F6, F7 — verified against current artifacts), **2 Dismissed-by-structure but substantively Open** (F4, F5 — Round 1 fix cycle did not address these; re-raised), **2 Dismissed** (F8, F9 — Round 1 dismissals hold), **1 new Open finding** (F10 — fix-cycle-introduced literal-expected-output defect in new Step 0), **0 Hallucinated**.

**Substantive new-finding tally:** the Round 2 pass produced (a) F4 + F5 surviving from Round 1 without resolution (which the [Phase 3 primer](../../../../vsdd-suite/primers/3-review-session.md) § Continue trigger treats as "still real findings, not closed") and (b) one new fix-cycle-introduced finding (F10). Per the primer's continue-trigger framing: **substantive findings remain; the UX domain is NOT at MVR at the close of Round 2; Round 3 is mandatory.** The MVR signal is NOT triggered — the layer-at-MVR criterion ("the round *after* the last new-finding round produces only Hallucinated findings or no findings") requires a future Round 3 that produces only Hallucinated/no findings against the post-Round-3-fix-cycle state.

**MVR signal explicit declaration:** Round 2 produces 3 substantive findings (F4 re-raised + F5 re-raised + F10 new). **The UX domain is NOT at MVR.** Round 3 is mandatory per [G-131](../../../../vsdd-suite/suite-development/FINDINGS-INDEX.md#g-131) continue trigger discipline. The expected Round 3 trigger-check evidence: (a) F4 fix lands (`long_about` / `after_help` on the `#[command(...)]` block at [`../../src/main.rs`](../../src/main.rs):27-32 with a 2–3-line usage block); (b) F5 fix lands (remediation-hint suffix at the four storage-error sites at [`../../src/main.rs`](../../src/main.rs):95, 103, 107, 116); (c) F10 fix lands (Step 0's expected `which bm` block reshaped per one of the three resolution options above).

**Coordination:** F4 + F5 route to the [Software Engineer](../../../../vsdd-suite/domains/role/SOFTWARE-ENGINEER-REVIEW.md) log at the next SE round for code-change validation (clap derive attribute + error-format remediation suffix). F10 routes to the [Quality Engineer](../../../../vsdd-suite/domains/role/QUALITY-ENGINEER-REVIEW.md) log at the next QE round for runnable-manual-test fidelity validation (same validator pair as Round 1 F7). The Round 1 F1, F3, F6, F7 resolutions are jointly validated by QE — the fixes ride through QE's runnable-manual-test discipline pass at the next QE round (per Round 1's Coordination line). The Round 1 F2 resolution is jointly validated by [Sanity Check](../../../../vsdd-suite/domains/meta/SANITY-CHECK-REVIEW.md) as a spec-heading clarity finding without a natural domain pair. No new cross-domain implications to [Security](../../../../vsdd-suite/domains/role/SECURITY-REVIEW.md), [Performance Engineer](../../../../vsdd-suite/domains/role/PERFORMANCE-ENGINEER-REVIEW.md), [Platform Engineer](../../../../vsdd-suite/domains/role/PLATFORM-ENGINEER-REVIEW.md), [Red Team](../../../../vsdd-suite/domains/role/RED-TEAM-REVIEW.md), or [Technical Writer](../../../../vsdd-suite/domains/role/TECHNICAL-WRITER-REVIEW.md) surfaces beyond what Round 1's Coordination already named — F10 specifically does not cross-touch TW because the defect is in a runnable-manual-test step (QE) rather than a TW-owned doc artifact.
